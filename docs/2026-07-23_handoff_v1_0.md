# Boomtown Platform — Session Handoff v1.0
**File:** docs/2026-07-23_handoff_v1_0.md · **Date:** 2026-07-23 · **Supersedes:** docs/2026-07-23_handoff_v0_9.md
**Purpose:** everything a fresh Claude session needs. Read this + CHANGELOG first. Older handoffs (v0.8, v0.9) are condensed here — do not re-read them.

## 1. State of the build
- **v0.7.0 + v0.8.0 exist in code only** (ZIPs `2026-07-23_boomtown_v0_7_0.zip` and `2026-07-23_boomtown_v0_8_0.zip`); owner has NOT pasted either yet — live is still v0.6.0. Paste order matters: **v0.7 first, then v0.8** (v0.8's dashboard reads migration-0005 columns and v0.7 endpoints).
- **v0.7.0:** League Manager (level-gap scheduler), Sales & Reports, member notifications + bell, teammate connect/invite, retry-payment, daily cron, event staff assignment, nav v2.0 (fixed rail/logo/icons/collapse/Back), tokens v0.2 contrast fixes, member dashboard home.html.
- **v0.8.0 (this session):** Control Center — `/api/admin/dashboard` (reports.js v1.1: month money card+cash, outstanding list, 7-day trend, schedule w/ staff, member count, admin alerts), admin.html full rewrite + admin-dash.js (KPI row, Today & Next Up w/ LIVE flag, Money Outstanding with inline Remind/Rerun, 7-day bar chart, Needs Attention), admin.css v0.5.0 streamline (18px cards, 1280px cap, quieter tables). Pattern study: Gymdesk dashboard concepts (payments/attendance/schedule/tasks/overdue) — implementation, copy, and styling are entirely original on Boomtown tokens.
- **⚠️ Migration 0005 STILL not applied to live D1** (Cloudflare MCP auth incomplete two sessions running). First action next session: re-auth Cloudflare connector → run `db/2026-07-23_0005_leagues-notifications_v1.0.sql` on `boomtown-prod` (id 6cde5d11-4199-4e57-b10f-2b7e968264ea); then seed `db/2026-07-23_seed-testdata_v1.0.sql` (TEST rows IDs 90000+, CLEANUP block, never renumber). Owner fallback: D1 console (module 8 doc §1).

## 2. Environment facts (do not rediscover)
- Repo **10xequity/btplatform** → 10xequity.github.io/btplatform. GitHub MCP write = 403 → ZIP + owner pastes in web editor; binaries (logo.jpg) via "Upload files". Worker `boomtown-api` auto-deploys via GitHub Actions on push. Cron `0 15 * * *`.
- D1 `boomtown-prod`, R2 `boomtown-avatars`, Brevo + Square both sandbox until keys set. Cloudflare MCP = DB only.
- Owner non-technical: click-by-click docs, date+version header on every file, no filler, [FACT]/[INTERPRETATION] labels, verify UI steps by search.
- Passkeys rpId = 10xequity.github.io only. site-nav skips ?embed=1. Cache-bust with ?v= (new pages 0.8.0).
- Worker module pattern: `xxxRoutes(request, env, url, ctx)` + `wireXxx(helpers)`; helpers {json, audit, isStaff, requireStaff, sendLoginLink, issueSession}. Score contract POST /api/matches/:id/score {winner:'a'|'b', diff≥1}. Teams bulk add {names:[…]}.

## 3. Owner-approved module roadmap (recommended 2026-07-23, build in this order)
1. **M10 Check-in & attendance** — QR/day-of check-in per event, roster view w/ waiver flags at the door, attendance history per member (feeds retention). Industry table-stakes (Gymdesk/Mindbody core loop).
2. **M11 Memberships & recurring billing** — monthly membership plans (vs per-event), Square subscriptions, auto-retry failed payments, member-count-based revenue. Biggest revenue unlock.
3. **M12 Marketing & comms** — email blasts to segments (level, program, lapsed), lead capture form, automated follow-ups (didn't finish registration / hasn't played in 60 days). Uses existing Brevo.
4. **M13 POS-lite** — merch/day-pass quick sale on Square, inventory count, shows in Sales & Reports.
5. **M14 Waitlists & capacity** — event caps, auto-promote from waitlist, refund on withdraw.
6. **M15 Member mobile polish + PWA** — manifest, install prompt, push notifications (ties to bell).
7. **Phase-3 carryovers:** forum, photo galleries (R2), rate limiting, error monitoring, verified change-email, passkey list() field-name confirmation, REAL waiver text (blocks go-live), Brevo key + DKIM, Square production keys + webhook.

## 4. Files changed in v0.8.0
Worker: index.js (v0.8.0 health), reports.js (v1.1 dashboard), wrangler.toml (v0.8.0).
Web: admin.html (v0.8.0 rewrite), assets/admin-dash.js (NEW v1.0), assets/admin.css (v0.5.0).
Docs: CHANGELOG v0.8.0, 2026-07-23_module9-install_v0.8.0.md, this handoff.
(assets/admin.js retained but unused by admin.html.)

## 5. Chat hygiene (standing instruction)
Monitor context each session; nearing ~80% → new chat + handoff superseding this file. This handoff v1.0 already condenses v0.8 + v0.9 — treat those as archived.
