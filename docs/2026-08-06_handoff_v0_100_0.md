# Boomtown Platform — Session handoff

**File:** `docs/2026-08-06_handoff_v0_100_0.md` · **Version:** v1.47 · **Created:** 2026-08-06
**v1.47 (iteration 39, 2026-08-09) — SHIPPED v0.115.0: THE OWNER'S "CANNOT CONNECT TO SERVER" BUG, DIAGNOSED AND FIXED. It was our CORS header, and our error message blamed his wifi.** Suite **1533/1533** (1527 + 6), **97** test files, 51 modules, busters **402 across 65 files** at 0.115.0, ledger **0043** unchanged (no migration). `/api/health` flapped once to v0.114.0 then held **11 consecutive**; never v0.100.0. Pages 0.115.0; both preflight checks PASS. `git commit` and `git push` FIRST attempt, **seventeenth push running**. **THE CAUSE:** `corsHeaders` advertised `Access-Control-Allow-Methods: "GET,POST,OPTIONS"` while the worker routes **45 handlers on PATCH, PUT and DELETE across more than twenty modules** (measured: GET 148, POST 165, PATCH 12, PUT 12, DELETE 21). A cross-origin DELETE is preflighted; the browser read that header, did not find DELETE, and **refused the request locally** — so `admin-nav.js api()`'s catch fired with *"Can't reach the server. Check your connection and hard-refresh (Ctrl+F5)."* **`fetch` only throws at the network or CORS layer, never for a 4xx or 5xx**, so the single message the owner saw pointed at his connection while the fault was a string in our worker. Every Delete button and most Edit actions were dead in a real browser — and "several modules, not all" was the whole clue: the split was the HTTP VERB, so GET/POST screens kept working. **VERIFIED BY REPLAYING THE BROWSER'S OWN PREFLIGHT AGAINST PRODUCTION, before and after** — `OPTIONS /api/admin/programs/1` with `Access-Control-Request-Method: DELETE` returned `GET,POST,OPTIONS` before and `GET,POST,PUT,PATCH,DELETE,OPTIONS` after, while `evil.example` still receives **zero** allow-origin headers. **NO TEST COULD HAVE CAUGHT THIS BY EXERCISING ROUTES** — the suite calls the worker directly over the shim and a preflight is a browser behaviour that never happens there, so 1,527 tests passed against a product whose Delete buttons did not work. The new guard therefore **extracts the routed methods from the modules and asserts the header covers them**: a relationship between two lists read from one source, which cannot drift, where a hardcoded verb list would have gone stale at the next new verb. **SETTLED BY THE OWNER, 2026-08-09:** the sandbox login stays as it is **until go-live, as the last item on the launch checklist** — recorded as a decision with a trigger, not an open question. Its prerequisite stands: closing it needs a passkey enrolled or `BREVO_API_KEY` set first, or it is a lockout.
**v1.46 (iteration 38, 2026-08-09) — NO RELEASE, AND NOT SHIPPING WAS THE DECISION. S-2a-bis IS ANSWERED FROM DATA AFTER SEVEN SESSIONS OF ASKING: `BREVO_API_KEY` IS NOT SET, THE SYSTEM RUNS IN SANDBOX MODE, AND THE §-1i S-3a / S-4b FAIL-OPEN IS LIVE.** Build state untouched and re-verified — preflight CLEAR, suite **1527/1527**, 96 files, v0.114.0 live on both pipelines, ledger 0043, in sync. Docs-only, so no bump and no sweep. **THE QUESTION WAS FILED AS "UNVERIFIABLE FROM HERE" AND WAS ANSWERABLE THE WHOLE TIME.** `magic_links` records `created_at` and `used_at`: **all nine completed sign-ins consumed their link within 1–11 SECONDS, median 3.** Nobody receives an email, opens a mail client, finds the message and taps a link in three seconds — nine times out of nine. `app.js:196-197` then settles it: on `mode === "sandbox"` the login screen renders *"Sandbox mode (no email provider yet)"* **with the sign-in link inline as an anchor**, which is exactly what a three-second sign-in looks like. Third signal agreeing: **zero campaigns ever sent**, and `marketing.js` is Brevo-gated too. **CONSEQUENCE: anyone who knows the URL can request a sign-in link for the owner's address and receive a working token in the response body.** **THE FIX WAS NOT SHIPPED, DELIBERATELY, AND THAT IS THE SESSION'S REAL DECISION.** S-4b gates the sandbox branch behind `SANDBOX_LOGIN === "1"` and 503s otherwise — and with no mail key and **zero passkeys enrolled**, that branch is the owner's ONLY route into his own system. Shipping it tonight would have been a pre-authorised, guarded, fully tested **lockout**. **ENUMERATE THE EXITS, the trap v0.107.0 already paid for, this time where the exit was not a button but the whole authentication method.** **REQUIRED ORDER NOW FIXED: the owner enrols a passkey (live since v0.5.0, no email involved, thirty seconds) or sets the mail key → the new route is verified working → THEN S-4b ships.** Also recorded: the product had been stating the answer in plain English on every sign-in and the owner read it as a status line — **"is BREVO_API_KEY set?" is unanswerable to him; "does your screen say Sandbox mode?" is trivial.** The framing was the defect, and it was asked seven times.
**v1.45 (iteration 37, 2026-08-09) — NO RELEASE. The loop PROMPT was the unit: it had been rejected twice by Fable 5, which blocks every other item.** Build state untouched and re-verified — preflight CLEAR, suite **1527/1527**, 96 files, v0.114.0 live on both pipelines, ledger 0043, in sync. Docs-only, so no bump and no sweep. **THE REFUSAL WAS NOT THE SECURITY CONTENT — I tested that hypothesis and it was wrong.** Fable accepted the fail-open / escalation / enumeration section verbatim, reasoning that describing a vulnerability's mechanism is inseparable from fixing it, that every actionable instruction in it is a repair, and that it even carries a protective do-not-probe guardrail. **THE REAL TRIGGER WAS A STANDING RULE I WROTE MYSELF THAT OVERRODE THE OWNER'S OWN CLAUDE.md:** "migrations via Cloudflare MCP BEFORE the push" made an irreversible live-production schema change the default automatic behaviour, while the global instructions say to ask first about anything hard or impossible to undo. **The model was right to refuse, and I had not noticed across many iterations that my own rule had quietly overridden a standing safety instruction.** Repaired with CLASS-SCOPED authority rather than weaker authority: **additive migrations (CREATE TABLE / ALTER TABLE ADD COLUMN) stay pre-authorised** — reversible in practice, nothing reads the new column until its code ships, ledger read back immediately — while **DROP / RENAME / REWRITE / BACKFILL stops and shows the SQL and the row count first**. Also fixed: **"a 403 is not an outage" was true for READS and dangerous as written** — a failed WRITE is now never blind-retried (re-read the row, stop on ambiguity), because a retried partial write is how one bad row becomes two. And a collision neither I nor the prompt had reconciled: **CLAUDE.md forbids changes touching more than ~5 files while S-4b requires editing 26 test envs** — now granted explicitly and scoped to mechanical harness edits. Re-verified against the model that refused: all three revised sections ACCEPT, overall PROCEED.
**v1.44 (iteration 36, 2026-08-09) — SHIPPED v0.114.0: TWO AUTH SURFACES THAT REPORTED SECURITY THEY DID NOT HAVE. Plus §-1i S-4, the go-live auth answer, from an eight-agent adversarially-verified audit.** Suite **1527/1527** (1516 + 11), **96** test files, 51 modules, busters **402 across 65 files** at 0.114.0, ledger **0043** unchanged (no migration). `/api/health` v0.114.0 — flapped with v0.113.0 across the first ten samples, then **12 CONSECUTIVE**; never v0.100.0. Pages 0.114.0; both `preflight` checks PASS. Live security re-verified after the change: `passkey/login-options` 200, `passkey/remove` anonymous **401**. `git commit` and `git push` FIRST attempt, **sixteenth push running**. **THE OWNER'S PREMISE WAS WRONG IN BOTH DIRECTIONS AND THAT WAS THE SESSION.** *"Currently, there is a 2fa"* — **there is not.** The admin Users screen rendered a "2FA" column from `users.totp_enabled`, a column **read in two places and written in NONE**; it could only ever say "Off", forever, so an admin reading it concluded two-factor existed and was merely switched off. TOTP was planned and REPLACED by passkeys; the plan was dropped and the column outlived it. *"Can we set up a passkey?"* — **it is already built, live and unused.** `webauthn.js` is 359 lines of real WebAuthn (`crypto.subtle.verify` over `authData || SHA-256(clientDataJSON)`, COSE→WebCrypto import, RP-ID hash, origin, single-use purpose-bound challenges, counter clone detection) feeding the SAME `issueSession` the magic link uses; `passkey.js` calls `navigator.credentials` create and get from the login page. **Live D1: `webauthn_credentials` holds ZERO rows.** **SECOND DEFECT, SAME THEME:** the passkey **Remove** button read `k.id` and `k.nickname` — three field names `list()` has never returned — so it posted an empty credential id, matched no row, and `remove()` returned `{ok:true}` **and wrote a `passkey.remove` AUDIT ROW anyway. The security log recorded removals that never happened.** Both halves fixed: the screen reads `credential_id`/`device_label`, and `remove()` now checks `meta.changes`, 404s when nothing matched, and audits only after a real change. **§-1i S-4 answers the go-live question: passkey-first with magic link as recovery — build NO new auth — and OAuth is the wrong build** (the frontend is a PATH on a shared github.io origin; **the deploy workflow carries NO secrets at all**, so any new one is hand-set like BREVO; and account-linking by email is unsafe without requiring `email_verified`). **Three hardening items owed: S-4a user verification is computed and never read** (`userVerified` at `:217`, one repo-wide hit — so a bare touch passes while the UI promises "both factors"); **S-4b the fail-open is WIDER than S-3a said** — `security.js rescueLink` spreads the same body, so in sandbox mode any STAFF user can mint and read a sign-in link for an ADMIN, and **the fix must land inside `sendLoginLink`, not the route**; **S-4c the passkey crypto has zero tests.**
**v1.43 (iteration 35, 2026-08-09) — SHIPPED v0.113.0: §-1h M-1, wide admin tables scroll instead of squashing on a phone. Plus §-1i, the SECURITY REVIEW, whose top finding is the most urgent open item in the project.** Suite **1516/1516** (1509 + 7), **95** test files, 51 modules, busters **402 across 65 files** at 0.113.0, ledger **0043** unchanged (no migration). `/api/health` v0.113.0 on **10 consecutive samples, no flap, never v0.100.0**; Pages 0.113.0 **and the live `app.css` on Pages was fetched and confirmed to carry the new rule** — the version matching is not the same as the CSS being there. `git commit` and `git push` FIRST attempt, **fifteenth push running**. **Owner approved §-1h M-2's six QOL items** (§5 item 7 closed) and asked for security suggestions. **THE MOBILE FIX:** `.tbl` is `width:100%` under `body { overflow-x: hidden }`, and a width-100% table cannot overflow — it compresses, so `admin-reports`' seven-column revenue table got ~55px a column at 390px with the money columns losing; 12 of 14 table pages affected. **The obvious fix was wrong:** `app.css`'s existing `.score-table` card pattern hides `thead`, which on a seven-column financial table leaves a stack of unattributable numbers — worse than squashed, because a squashed number is still labelled. So the table scrolls ITSELF (`display:block` + `overflow-x:auto` + `nowrap`) with **no markup change on twelve renderers**, and `body { overflow-x: hidden }` is untouched so the PAGE still never moves sideways. **§-1i's headline, S-3a — `sendLoginLink` IS FAIL-OPEN:** when `BREVO_API_KEY` is absent the endpoint returns `dev_link`, a valid sign-in token for ANY email address, to ANY anonymous caller — total account takeover with no vulnerability beyond asking. **The shape is wrong even if the key is set**, because a security property must never depend on an unrelated secret being PRESENT. **NOT rushed into this release: 26 test files depend on `dev_link` and each builds `env` inline, so failing closed is a real sweep, and a login change is the last thing to ship in a hurry.** **The owner settles the urgency in ten seconds — do you receive sign-in emails? —** which makes §5's long-open S-2a-bis the highest-value unanswered question in the project. Also found: **no rate limit on the one unauthenticated write** (`/api/auth/request-link`), while six other modules already carry flood guards; and **no security headers** (no CSP, X-Frame-Options, Referrer-Policy) outside `uploads.js`/`calendar.js`. **What is already strong is recorded first in §-1i so the gaps read in proportion:** 256-bit tokens, hashed at rest, single-use, expiring, revocable sessions, no user enumeration, allowlist CORS, 182 audit call sites, and no passwords anywhere.
**v1.42 (iteration 34, 2026-08-09) — SHIPPED v0.112.0: the best-of-3 ADVANCEMENT ENGINE (stage one of two), plus the interface/mobile review the owner asked for.** Suite **1509/1509** (1497 + 12), **94** test files, 51 modules, busters **402 across 65 files** at 0.112.0, ledger **0043** unchanged (no migration). `/api/health` v0.112.0 on **10 consecutive samples, no flap, never v0.100.0**; Pages 0.112.0; `preflight` `deployed` and `pages` both PASS. `git commit` and `git push` FIRST attempt, **fourteenth push running**. **Owner answered three of the four §5 questions:** schedule the best-of-3 games — **YES** · model pool time — **"we dont need to check it"**, CLOSED · delete the superseded docs — already done in iteration 32. **THE FOURTH ANSWER DOES NOT ANSWER THE QUESTION AND HAS NOT BEEN ACTED ON:** §5's cross-org item asked *which of the three* org-scoped modules should go universal and whether the owner accepts republishing member profiles and silently breaking blocking; the reply was "Yes", which selects nothing among three mutually exclusive options with a privacy consequence. **Re-asked narrowly, not inferred.** **STAGE ONE IS THE ENGINE:** `gamesNeeded` + `matchWinnerOf` (first to N games, decided IN GAME ORDER, by games and never by points — a team can lose two close games and win one blowout) and `pendingAdvances` now GROUPS game rows into nodes before deciding a winner. Without that grouping the old loop saw three rows at identical coordinates and produced **one advance per game**, promoting game one's winner and then overwriting it — a bracket that quietly puts the wrong team in the final, passing every existing test because every existing fixture has one row per node. **Backward compatible BY CONSTRUCTION:** a node's format is read from the ROWS PRESENT, never from the template — defaulting a semi to best-of-3 would demand two game wins from every bracket already live in D1, all of which are single-row, freezing all of them on deploy. **STAGE TWO (the writer) IS DEFERRED WITH EVIDENCE, NOT AS AN EXCUSE:** `courts.js allocate()` contains ZERO occurrences of `game_number` and its only sequencing constraint is per-bracket-per-stage, so two games of one match would be placed in the **same wave on different courts** — the same two teams playing themselves simultaneously. Sequencing a node's games is a scheduling-correctness change to the allocator and is its own unit. **Nothing user-visible claims best-of-3 games exist yet.** **INTERFACE REVIEW recorded as roadmap §-1h**, and its headline is that **my own first scan was wrong** — "9 pages with no mobile breakpoint" was a false positive against `repeat(auto-fit, minmax(…))`, which is responsive by construction. Re-measured: **59/59 pages carry a viewport meta, 0 rigid sidebars lack a breakpoint**, and the one real defect is that **12 of 14 table pages squash rather than scroll** on a phone — `.tbl` is `width: 100%` under `body { overflow-x: hidden }`, so `admin-reports`' seven-column revenue table gets ~55px a column at 390px. **The fix already exists in the repo** (`app.css`'s `.score-table tr { display: block }` card pattern) applied to exactly one table.
**v1.41 (iteration 33, 2026-08-08) — SHIPPED v0.111.0: §-1c D-17b, THE MARKER SWEEP. The most owed item in the repo is closed, and it produced instance seven on the way.** Suite **1497/1497** (1492 + 5), **93** test files, 51 modules, busters **402 across 65 files** at 0.111.0, ledger **0043** unchanged (no migration). `/api/health` v0.111.0 — **one flap to v0.110.0 on sample 7, then 12 CONSECUTIVE**, converged; **never v0.100.0**, the zombie discrimination held. Pages 0.111.0; `preflight` `deployed` and `pages` both PASS. `git commit` and `git push` FIRST attempt, **thirteenth push running**. **D-17b had been recorded SIX times across five sessions, twice inside guards written to respect the rule, and twice by me. A defect recorded six times is not a defect, it is a missing check** — so the unit is `worker/test/marker_hygiene.test.mjs`, which scans every test file for the two mechanically detectable costumes: a quantifier applied to `[\s\S]`/`[^]`/`.` (a distance between two spellings) and a source slice of 50+ characters. **Its first run accused 13 instances across 7 files and all 13 are re-anchored** on brace-matched or syntactically-bounded regions — announcements (3), divisions_page (2), live_board (1), live_motion (1), login_brand (2), marketing (1), passes (3). It deliberately does NOT flag `[0-9a-fA-F]{3,6}` (a hex colour), `h.slice(i, i+2)` (a hex pair), `bits.slice(i, i+8)` (a byte) or brace matchers — **a noisy checker gets an allowlist, and an allowlist is where a real instance hides.** **THE SWEEP PRODUCED INSTANCE SEVEN AND THE EXISTING SUITE CAUGHT IT:** my first fix anchored on `kind === "cta"`, which occurs FOUR times in `announcements.js`, and `indexOf` returned the feed's filter instead of the mute route's refusal — the old regex was immune because **a regex SCANS and an index does not**. Every anchor introduced now asserts its own uniqueness first. **Also found and NOT silently patched: `functionRanges`/`statementFrom` mis-read a body when the parameter list holds a default object** (`function png(text, opts = {})` hands them `{}` as the block). `functionBodyAfter` was added and the limitation recorded as **§-1c D-20**, because two security guards consume `functionRanges` and changing it is its own unit. **Vacuity checked, not assumed:** breaking `web/assets/qr.js` reddened divisions_page (18/1) and the revert went green (19/0).
**v1.40 (iteration 32, 2026-08-08) — SHIPPED v0.110.0: THE STANDARD TOURNAMENT TEMPLATE AND THE 16-GAME CEILING. Five superseded docs DELETED with owner approval.** Suite **1492/1492** (1481 + 11), **92** test files, 51 modules, busters **402 across 65 files** at 0.110.0, ledger **0043** unchanged (no migration). `/api/health` v0.110.0 on **10 consecutive samples, no flap, never v0.100.0**; Pages 0.110.0; `preflight` `deployed` and `pages` both PASS. `git commit` and `git push` FIRST attempt, **twelfth push running**. Three-way live control: health 200, `preview` anon **401**, fabricated sibling route **404**. Owner, verbatim: *"generally in a standard tournament template - we would aim to run 8 games in pool play, break everyone then best of 3 matches quarters to finals. Usually though, we have 9-10 ROUNDS (not games) so we hit the 8 but ten due to time, we do 1 game quater finals to 25, then 2 mathes best of 3 for semi and finals. This way the max games players are playing are approximately 12-16. More than 16 become physically unplayable."* and *"each match taking 20 minutes, Each 15 pt takes 15 minutes (3rd game of a match)."* **FOUR ADDITIONS:** (1) **ROUNDS ARE NOT GAMES** — 9–10 rounds give 8 games because a bye is a round you do not play; `formats.js` always computed `2CR/N`, and the distinction now survives into the wording. (2) **THE BRACKET IS NOT ONE FORMAT** — `BEST_OF_3_FROM_ROUND = 2` states "semi and final best-of-3, quarters and earlier one game to 25" exactly, because `bracket_round` already counts backwards from the final. (3) **A CEILING, NOT ONLY A FLOOR** — `MAX_GAMES_PER_TEAM = 16`, and it is checked **BEFORE** the floor, because telling a director they are one game short while the winner is on for eighteen makes the day worse. (4) **TIME IS DERIVED** — `minutesForRound` computes from `BEST_OF_3_GAMES - 2`, so the 2.25 and the 23.75 come from ONE assumption and cannot drift apart on the same screen. The owner's worked example is asserted directly: 8 + 1 + 2.25 + 2.25 = **13.5**, inside the stated 12–16 band. **DOC HYGIENE:** the five v0.1-era docs are **deleted** (docs/ 21 → 16 `.md`); INDEX.md's four rows became one deletion record; **historical citations in CHANGELOG.md, LOOP.md and docs/archive/ were left intact deliberately** — those are dated records, and rewriting them would falsify history rather than tidy it.
**v1.39 (iteration 31, 2026-08-08) — SHIPPED v0.109.0: GAMES, NOT MINUTES. The owner reversed v0.108.0's core unit and this is the correction.** Suite **1481/1481** (1471 + 10), **92** test files, 51 modules, busters **402 across 65 files** at 0.109.0, ledger **0043** unchanged (no migration). `/api/health` v0.109.0 on **10 consecutive samples, no flap, never v0.100.0**; Pages `?v=0.109.0`; `preflight` `deployed` and `pages` both PASS. `git commit` and `git push` FIRST attempt, **eleventh push running**. Owner, verbatim: *"however do not use time as the core unit of measure"* · *"We aim at roughly 8 games x 25 pts in pool play before cutting anyone. If they receive less than that, for example 6 or 7, then everyone needs to break to meet the game minimum (8 games) that the first bracket games should fulfill"* · *"Game matches (best of 3) are considered 2.25 (since there's a 25% chance of it going to 3 games)"* · *"Please ensure brackets are scored by pool play, that is the whole point of pool play"* · *"We try to break everyone possible to give them as many games as possible (with a time constraint). We try to finish tournament within 7 to 8 hours (6 is ok with less teams)."* **THE MEASURE IS GAMES PER TEAM AGAINST A FLOOR OF 8.** The preview now reports `pool_games_per_team` (counted from real rows, MIN — the floor is a promise to the worst-off team), `guaranteed_games` = pool + ONE bracket match (1.0 single, **2.25 best-of-3**), `meets_minimum`, `games_short`. **A short field is told to BREAK EVERYONE, then to run the first round best-of-3 — never to cut to a top 8**, because cutting takes games from exactly the teams below the floor. Minutes are still reported; they are a boundary, never the verdict. **THE FLOOR HAS ONE DEFINITION:** `formats.js` already exported `MIN_GAMES_PER_TEAM = 8` and already refused uneven pools, so `brackets.js` imports it rather than restating an 8. **N-6 ANSWERED:** the entry-seed fallback stays (a bracket-only event needs it) but now announces itself via `seed_warning` — a bracket seeded from entry order is otherwise indistinguishable from one seeded off a real finish. **Three v0.108.0 tests asserted the old minutes-first rule and were REWRITTEN, not deleted.** **D-17b sixth instance, and mine:** my own new screen test pinned a 600-character window AND an ambiguous anchor that matched a different `const bits = [` 400 lines earlier; re-anchored on a brace-matched function body. **Best-of-3 is an ESTIMATE input only** — the generator still writes one match per node, so running it is the director's action at the table; auto-scheduling it is NOT built and is recorded in §5.
**v1.38 (iteration 30, 2026-08-08) — SHIPPED v0.108.0: §-1d, the timeframe estimate.** Suite 1471/1471, 92 files, ledger 0043. Owner answered "which knob when short" (**top 8**) and scope (**league-wide default**, division split only for a large unrated field). New `POST /api/admin/events/:id/brackets/preview` — writes nothing; `planFor()` extracted so preview and generation share ONE planner. **The agreement test earned its keep immediately by catching the generate ROUTE not forwarding the new `waves` field.** *(v1.39 supersedes this release's UNIT — minutes were the wrong measure — but the preview endpoint, the shared planner and the agreement test all stand.)*
**v1.37 (iteration 29, 2026-08-08) — NO RELEASE, DELIBERATELY. §-1d Shape A unblocked because its blocker did not exist.** Suite 1453/1453, 91 files. **There was no column to invent:** the end-of-league tournament is a bracket drawn ON the league event — `brackets.event_id` carries it, `generateBracketFor` never reads `events.type`, the picker lists every event, and `teams.event_id` makes the roster structural. Proven by INJECTING a type gate and watching 6 of 8 tests go red, then reverting.
**v1.36 (iteration 28, 2026-08-08) — SHIPPED v0.107.0: §-1f F-1, the real privilege drop. §-1f CLOSED.** Suite 1445/1445, 90 files, **migration 0043 applied via MCP and read back — MCP D1 WRITES VERIFIED** after six releases unverified. **"View as member" had been presentation-only since v0.15.0** — the server never knew, so an admin previewing kept every privilege and could not see the 403s a member hits, which is the one thing the feature exists for. `sessions.acting_role` is the seam. **A lockout was caught before shipping:** both exits cleared only the client flag.

**EARLIER HISTORY, CONDENSED (v1.31–v1.35, iterations 22–27) — full detail lives in `LOOP.md`, which is the ledger of record.** v1.35/v0.106.0: per-org login branding built by copying `applyOrgBrand`, the pattern the member rail had used since v0.50.0; **S-1b CLOSED and an inference I had recorded myself was REVERSED** — `/api/orgs` now requires a session while `/api/public/org-brand` stays public. v1.34/v0.105.0: the member rail's paint order (D-15) — **the recorded candidate mechanism was WRONG and one grep disproved it**; the real cause was a whole navigation column appended after three serially awaited fetches. v1.33/v0.104.0: cross-org isolation, §-1e S-3 — the owner's three "universal" modules were measured as **rigorously org-scoped already**, turning a bug report into an unbuilt feature with a privacy cost. v1.32/v0.103.0: the authorization matrix — **`requireAdmin` has exactly FOUR call sites in the whole worker** while ~190 gates admit staff; nothing exploitable at 0 staff roles, all of it live at the first hire. v1.31: docs-only, no release.

## §0. Delivery — ✅ **SUPERSEDED. CURRENT STATE IS §0b: v0.102.0 IS FULLY LIVE, BOTH HALVES.**

> **§0 is the iteration-18 record and §0a the iteration-19 record; both are kept for their failure
> signatures only. Do not act on either. Skip to §0b.**

> **§0 is the iteration-18 record, kept for the failure signatures only. Do not act on it.** Its
> headline ("committed but not deployed") and its recovery block are both out of date: iteration 19
> landed the worker via `workflow_dispatch`, and the queued re-run it tells you to prefer never got
> a runner in two hours. **Skip to §0a for the live state.**

| | |
|---|---|
| `main` | **b5d0322**, `v0.100.0`, pushed, clean tree, nothing unpushed |
| `/api/health` | **still `v0.99.0`** — 40 samples over 8 minutes, no flap, then CI examined |
| Cause | **GitHub Actions partial outage.** Confirmed against `githubstatus.com`: `Actions: partial_outage`, `Pages: degraded_performance`, open incident "Incident with Actions — investigating" |
| Failure 1 (run 31118355010) | `gate` job sat **15m** and died: *"The job was not acquired by Runner of type hosted even after multiple attempts."* `record-changelog` and `deploy` never ran (0s each) |
| Failure 2 (same run, re-run) | *"Failed to resolve action download info"* + *"Service Unavailable"*, both in **Set up job** — before a single line of repo code executes |

**Nothing is at risk in this state, and that is checkable rather than hopeful:** this release adds
**no migration** (ledger 0042, 42 files, unchanged), so there is no applied-migration/undeployed-code
skew — the standing hazard the schema gate exists for. The change itself is the sandbox **fixture**
plus a `tierOf` export; no live behaviour depends on it. The buster sweep moved `web/**` to
`0.100.0`, and if Pages deploys before the worker the only difference is a query string on assets
that are byte-identical. **The state is "not live yet", not "inconsistent".**

**THE RECOVERY IS ONE COMMAND, once GitHub Actions is healthy** — no code change, no new commit:

```bash
gh run rerun 31118355010 --failed          # preferred: re-runs the failed jobs in place
gh workflow run deploy-worker.yml          # fallback: workflow_dispatch IS enabled
gh run view 31118355010 --json status,conclusion -q '.status+" "+(.conclusion//"-")'
```

**An empty commit will NOT trigger a deploy.** `deploy-worker.yml` fires only on `worker/**`,
`wrangler.toml`, `db/migrations/**`, or the workflow file itself — plus `workflow_dispatch`. This
was verified this session, because the docs commit below correctly produced no Deploy Worker run
and that is by design, not another symptom of the outage.

**The outage was still WORSENING when this session ended:** it went from `Actions: partial_outage`
to **`Actions: major_outage`, `Pages: major_outage`** over roughly thirty minutes. Do not assume it
was brief — check the status page before spending a re-run.

Then sample `/api/health` to convergence as usual. **Do not `wrangler deploy` by hand** — that
bypasses the CI schema gate, which is a guard, not a formality.

**Two deploy attempts is where I stopped.** Retrying into a *confirmed, still-open* platform
incident proves nothing and burns the session. Check `githubstatus.com` before the first re-run.

### §0a. ITERATION 19 — ✅ **v0.100.0 IS LIVE ON BOTH PIPELINES. NOTHING IS OWED.**

**Read §0 above as history — it is superseded by this section.** At 20:00Z run **31127256344**
(`workflow_dispatch`, sha `bc253d3`) went green on all three jobs — `gate`, `deploy`,
`record-changelog` — and `/api/health` **converged on `v0.100.0`, 5 consecutive samples, no flap.**
`preflight deployed` now PASSES. `record-changelog` was correctly a no-op; the v0.100.0 entry was
already present and `--check` confirms it.

**What actually unstuck it:** the queued re-run of 31118355010 never got a runner (still queued at
close, ~2h). **`gh workflow run deploy-worker.yml` did** — dispatch found capacity when the queued
push-run could not. The push of `bc253d3` (which touches `worker/**` and should have triggered a
run) **produced no Deploy Worker run at all** — run *creation* was degraded, not just execution.
**If a push produces no run during an outage, that is not the paths filter, it is GitHub — dispatch
instead of debugging the filter.**

**PAGES LANDED TOO, on the third attempt of the session.** Build `31127226769` (bc253d3) sat `queued` 16+ minutes and was eventually **cancelled**; the docs push (`aad4cae`) created build **31127479173**, which got a runner and went **green at 20:16Z**. Live `/web/` now serves **`?v=0.100.0`**, sampled to convergence alongside `/api/health` (5 consecutive, both). **`preflight` reports `deployed` PASS and `pages` PASS.** Final state:

| Half | Pipeline | State at close |
|---|---|---|
| Worker / API | `deploy-worker.yml` | **v0.100.0, live, converged** ✅ (run 31127256344, `workflow_dispatch`) |
| Static app | `pages-build-deployment` (GitHub-managed) | **0.100.0, live, converged** ✅ (run 31127479173, triggered by the docs push) |

**v0.99.0's W-F screens have now reached a browser for the first time** — they had never been live, because that release's Pages build failed while its worker went green. **The owner's tester round (§5) is unblocked**: both halves are current, so a tester on a hard refresh gets the v0.100.0 bundle against the v0.100.0 API. **The way Pages was recovered is worth keeping: it is GitHub-managed, has no `workflow_dispatch`, and the only lever is a push to `main` — the docs commit closing the session was the trigger.**

**⚠ ONE LOOSE END — a zombie run record, probably inert, recorded because "probably" is not
"provably".** Run **31118355010** (sha `b5d0322`, `run_attempt=2`) still renders as **`queued`** in
`gh run list` / `gh run view`, ~3h after the re-run that created attempt 2. It is almost certainly
dead, on three pieces of evidence: `updated_at` is frozen at **19:34:13Z** (the moment of the
re-run) and has not moved since; **its jobs list is empty** — GitHub never materialised a single
job, so there is nothing to hand to a runner; and `gh run cancel` refuses it with *"Cannot cancel a
workflow run that is completed"*, meaning GitHub's own write path considers it finished while its
read path still says queued. **It could not be cancelled, so it is documented instead.**

**The hazard if it ever does execute** (state it plainly rather than assume it away): it would
deploy `b5d0322`'s worker, and that commit's `index.js` says `v0.100.0`, so its own post-deploy
parity step would assert against `v0.100.0` and **pass** — a green run that silently rolls
production back. Today that is harmless: `git diff b5d0322 HEAD -- worker/src/ wrangler.toml
db/migrations/` is **empty**, so it would redeploy a byte-identical artifact. It only becomes a
rollback once a release bumps past v0.100.0.

**The existing ritual already catches it, which is why this is a note and not a task:** sampling
`/api/health` to convergence after a bump would show v0.100.0 instead of the new version and fail
to converge. **Do not skip the convergence sampling on the next bump**, and if it reads v0.100.0,
this is the first thing to suspect rather than a Cloudflare flap.


---

### §0a-i. How this was found — the check that could not fail

**Re-tested the blocker rather than routing around it** (last session's own rule, applied to a new
claim). The status page said `Actions: major_outage`, but *"the platform is degraded"* and *"my run
cannot start"* are different claims, and only the second one matters — **the status page cannot
speak to the second.** Splitting them is what produced the deploy: a re-run was accepted and
reached `queued` (already better than iteration 18's two hard failures), and a `workflow_dispatch`
then found a runner outright. **Both were free to try and neither was implied by the status page.**

**THE REAL FIND — the static app WAS two releases behind, and the check that should have caught it
could not fail.** When this session opened, `https://10xequity.github.io/btplatform/web/` served
**`?v=0.98.0`** against a repo at `0.100.0`. (Both are current now — §0a. The finding is the blind
spot, not the drift, and the blind spot is what got fixed.)

| Release | Worker (`deploy-worker.yml`) | Static app (`pages-build-deployment`) |
|---|---|---|
| v0.98.0 | green | **green — this is what browsers still run** |
| v0.99.0 | **green, reported "VERIFIED LIVE"** | **FAILED** (run 31116456744, 15:34Z) |
| v0.100.0 | **green — iteration 19, via `workflow_dispatch`** | **green — iteration 19, 4th attempt** |

So **v0.99.0's W-F work — the `event` segment filter, the picker, "Email these registrants" — went
nine hours and two releases without ever being in a browser**, even though `/api/health` said
v0.99.0 and the ledger recorded it verified. The API moved; the bundle did not. **That is the part
to keep: the ledger entry was not lying, it was reporting a true fact about the wrong half.**

**Why nothing caught it, and this is the reusable part:** the ship has **two halves on two
pipelines**. `deploy-worker.yml` ships `worker/**` to Cloudflare and asserts `/api/health`
afterwards. The static app is shipped by a GitHub-managed `pages-build-deployment` run this repo
does not own, does not gate, and cannot see. `/api/health` proves the **worker** and nothing else —
and the release ritual's Pages step fetched `https://10xequity.github.io/btplatform/` **(the repo
root)**, a redirect stub last edited 2026-07-22 that carries **no `?v=` buster at all** and returns
a byte-identical 200 whether Pages built today or never. **Failure class 3, exactly: a guard
narrower than its subject, reporting clean.** Verified, not assumed — `grep -c '?v=' index.html`
is 0, and `git log` shows the file untouched for two weeks.

**Fixed this iteration:** `preflight` grew a **`pages`** check (reads `/web/`, compares the live
buster to `versionFromIndex`, WARNs on mismatch **and on a body with no buster in it**), and
`RALPH.md` PHASE 3 step 7 now names `/web/` and says why. `preflight` currently reports it, which
is the check earning its keep on the first run.

**Nothing new is at risk.** No migration, no schema skew; the worker is a full-tree deploy, so
whichever pipeline runs next carries everything. But **the owner's tester round (§5) must wait for
BOTH halves** — a green `/api/health` alone would put them back on the v0.98.0 bundle.

**`git push` succeeded on the FIRST attempt this session** — the classifier pattern is not
universal. `git commit` also went first try. Retry up to three, but do not assume three are needed.

**The health URL is `https://boomtown-api.vvisuth.workers.dev/api/health`** — take it from
`worker/scripts/preflight.mjs:58`, not from memory.

### §0b. ITERATIONS 20–21 — ✅ **v0.102.0 IS LIVE ON BOTH PIPELINES. NOTHING IS OWED.**

**This is the current delivery state. §0 and §0a above are history, kept for failure signatures.**

| Half | Pipeline | State |
|---|---|---|
| Worker / API | `deploy-worker.yml` (run **31193619665**, push) | **v0.102.0, live, converged** ✅ |
| Static app | `pages-build-deployment` (run **31193614689**) | **0.102.0, live, converged** ✅ |

Commit `2729e24`. `git commit` and `git push` both **first attempt**; **one push created both runs**
and both were green in about a minute — the healthy baseline, and now the pattern two releases
running. `preflight` reports `deployed` **PASS** and `pages` **PASS**.

**Convergence sampling earned its keep again.** `/api/health` read **v0.101.0 on samples 1, 3 and 5**
before settling — the documented ~2-minute edge flap, and proof that any single fetch in the first
minute would have reported the old version with total confidence. Pages was stable from sample 1.

**THE ZOMBIE STILL HAS NOT FIRED, and this release is the first that could have caught it cheaply.**
Run 31118355010 (§0a) would deploy **v0.100.0** and pass its own parity check. The flap above was to
**v0.101.0, never to v0.100.0** — which is exactly the discrimination the sampling exists to make.
The run still renders `queued` and still refuses cancellation. **Keep sampling; the hazard is
unchanged and its defence remains a habit, not a task.**

**A LIVE PROBE, because the ratchet is static analysis and cannot speak for the deployed worker:**
anonymous `GET /api/admin/permissions` now returns **401 `{"error":"Sign in first."}"`**, and the
control — anonymous `GET /api/health` — still returns **200**. Both halves of that check matter:
without the control, a 401 could just as easily mean the worker was down.

**The two checks preflight cannot self-verify were settled by hand, not assumed** (both iterations):
`git fetch` + `git log main..origin/main` **empty**, `git status -b` clean · live D1
`schema_migrations` via Cloudflare MCP: **MAX=42, 42 rows**, matching the repo's 42 files.
**Neither release adds a migration.**

**Iteration 20 was a recovery** and is recorded in full in `LOOP.md`: the session before it built
the N-1 fix, bumped to v0.101.0 and swept, then ended without committing — 67 files uncommitted,
which reads as a D3 drift halt but was this loop's own interrupted work. It was read, verified
coherent, and finished from release step 4. Nothing lost, nothing rebuilt.

---

## §1. Build state — measured 2026-08-09 (iteration 40, end of session)

| Thing | Value | How checked |
|---|---|---|
| Repo `main` | **v0.116.0** | `grep 'version: "v'`, byte-verified one-line diff |
| `/api/health` | **v0.116.0 — LIVE** ✅ | **12 samples: flapped to v0.115.0 twice (the PREVIOUS release — normal propagation), then 8 consecutive.** Never v0.100.0 — the zombie discrimination. `preflight deployed` PASS |
| **GitHub Pages `/web/`** | **`?v=0.116.0` — LIVE** ✅ | buster sampled AND the fixed bytes themselves fetched: live `admin-nav.js` carries `btn ghost sm bt-back` + the 16px svg rule; live `app.css` carries `touch-action: manipulation`. `preflight pages` PASS |
| Test suite | **1542 passing, 0 failing** | measured, never projected. 1533 → **1542** (+9, `human_chaos.test.mjs`'s own tests; no foreign re-registration) |
| Test files | **98** | F-37 parity gate. 97 → **98**: `human_chaos.test.mjs` is new |
| Worker modules | **51**, all parse | preflight `syntax` |
| D1 ledger | **0043**, 43 files in `db/migrations/` | read live via Cloudflare MCP 2026-08-09: `MAX(id)=43`, 43 rows, `MAX(version)='0043'`. **No migration this release** |
| Cache buster | single value **0.116.0**, **402** occurrences, **65** files | `sweep-buster.mjs --write` |
| **Cross-org probes** | **5 routes / 5 modules / 5 tables, all isolated** | `cross_org_isolation.test.mjs`. **80 id-taking admin routes exist — the other 75 are NOT cleared** |
| D-4 uncalled baseline | **15**, unchanged | the matrix adds no route |
| **Admin routes gated** | **102 of 102** | `admin_route_gating.test.mjs`, unchanged |
| **Admin-ONLY routes** | **3 dispatch sites of 102** | `authorization_matrix.test.mjs`. `requireAdmin` has **4 call sites in the whole worker**, all in `admin.js`; ~190 other gates admit staff |
| **Live roles (production D1)** | **3 admin, 0 staff, 1 user** | live MCP read 2026-08-08. This is why S-2 is a loaded gun and not a fire |

**On the version number:** v0.99.0 → **v0.100.0**, not 1.0.0. A three-digit minor parsed cleanly
through preflight, the buster sweep (400/64, one value) and the changelog writer. **Whether the
next milestone should be 1.0.0 is the owner's call** — see §5; nothing blocks on it.

---

## §2. What shipped this session

**v0.116.0 (iteration 40, owner request 2026-08-09) — the impatient-human release.** The owner asked
for tests that break the system the way a HUMAN does (clicking everything twice on slow loads,
pressing Back everywhere), the oversized back buttons fixed, a visual pass, and a member-landing
animation PLAN. Shipped: **(1)** the admin Back button — its class `bt-back` was defined in NO
stylesheet, so its viewBox-only SVG rendered at the replaced-element default (~300×150px): the
owner's "too big, wrapping, clipping" verbatim; now `.btn ghost sm` + 16px icon. **(2)** A real
double-submit hole the new probes caught: FREE events complete instantly as `comped`, which the
registration dup-guard never looked for — a double-click wrote TWO full registrations; now a
completed registration blocks a same-team-name resubmit while a genuine second team still passes
(`registrations.js`). **(3)** `human_chaos.test.mjs` (9 tests): double registration, sign-in link
clicked twice, a styled-class guard relating every class admin-nav.js injects to a real CSS rule
(cors_methods' two-lists shape), back-nav integrity at both `history.back()` sites, in-flight
button disabling on the three member write flows — every guard with an input-mutating NC.
**(4)** `api()`'s catch stops blaming the user's connection (roadmap M-2 item 7 holds the 14
sibling copies). **(5)** `touch-action: manipulation` on controls. **(6)** Roadmap v1.11: §-1h
M-4 — the member-landing motion pass, planned with values, NOT built. Guideline audit was
otherwise clean: no `transition: all`, reduced-motion honoured, inputs inherit 17px (no iOS zoom),
no bare `outline: none`.

---

**v0.102.0 — §-1e priority 1: S-1a closed, and the ratchet that keeps it closed.**

`GET /api/admin/permissions` was the **first dispatch line in `adminRoutes`, above every gate**, with
no session check of any kind — it returned the role list and the whole capability matrix to any
anonymous caller. **No org data and no PII**, so severity is low, but it published the authorisation
model and made "every admin route is gated" false rather than nearly-true. Gated with
**`requireStaff`, not `requireAdmin`** — the least that closes the disclosure and the only choice
that cannot break a working caller; the one caller is `admin-users.js:286`, on a page whose own data
is separately admin-gated, so tightening further is a **reviewable decision, not something to slip
in under cover of a security fix**. Verified live: anonymous GET now returns **401**, with
`/api/health` at 200 as the control.

**`admin_route_gating.test.mjs` — 102 dispatch sites, all three legitimate gating styles modelled.**
It imports `routesFrom` from `route_reachability.test.mjs` as the route oracle rather than parsing
the corpus a second way, and asserts the two extractions agree shape for shape (C14).

**THE FIRST RUN, BEFORE THE FIX, DID TWO JOBS AT ONCE.** It named the real defect — so it is not
vacuous — **and named nothing else across the other 101 sites**, so it has no false positives, where
two earlier hand-rolled scans produced 25 between them. That is the whole argument for writing the
guard before the fix and watching it fail.

**AND IT CAUGHT ITS OWN BUG ON THAT SAME RUN.** It reported a *second* ungated route,
`consent.js:503 /api/admin/media-consent/*`. **Reading the handlers settled it** — both gate on
their first line (`consent.js:400,442`). The scan was wrong, not the code: that module splits the
dispatch across two statements (`mt = p.match(/…/);` then `if (mt) { … }`) and a region stopping at
the `;` never reaches the handlers. Fixed by following the assignment to the `if` that tests it,
**precisely rather than by widening the region — an over-wide region lets a neighbouring gated route
vouch for an ungated one, and being too permissive is the worse failure because it reports clean.**

Four NCs, each breaking real source: re-open S-1a from the fixed file · strip `pos.js`'s single
module-level gate and prove **all 11** of its routes are exposed, not one · prove a gate DEFINITION
is not a call site (the exact error behind the original false "clean") · prove comment-blanking
preserves offsets and line numbers.

**On the +12:** 7 tests are this file's own; the other 5 are `route_reachability`'s, which
re-register because importing its export imports a test file. Chosen over parsing the route corpus a
second, divergent way (C14) or refactoring a working guard mid-unit. **Stated in the file header
rather than left as a puzzle in the numbers** — if a third consumer appears, move `routesFrom` to
`worker/testkit/`.

---

**v0.101.0 — §-1d N-1, the member/admin shell ping-pong. TWO mechanisms, and the second is the one
that actually traps you.** From the owner's 2026-08-06 evening report: *"members page v admin page
seems to switch back and forth and expose the admin page."*

**(a) The member sidebar advertised the admin shell.** `site-nav.js` v2.15 pushed a `Manage` group
of four admin destinations into the MEMBER nav for any staff viewer, while the admin header links
back to the member site — each shell offered the other. Deleted; the single `#btHdrAdmin` header
link is the way back. Same change drops the `|| roles[0]` fallback that resolved a caller's role in
**another** org when they held none in the org on screen. Presentation-only both times —
`requireStaff` re-checks `userId + orgId` server-side — but presentation is what was reported.

**(b) THE BACK-TRAP — the real cause, and a grep found it.** `grep -rn "location.replace" web/`
returned **nothing**: every bounce in the codebase used `location.href`, which **pushes** a history
entry. A bounce off an admin page therefore left `[..., admin.html, home.html]` on the stack —
press Back, the browser re-enters `admin.html`, **repaints the entire static admin shell** (that
page ships its rail and every module name as markup, so it is on screen before `/api/me` can
answer), and `guard()` bounces forward again. **Back never escapes and the shell is re-exposed on
every attempt.** `guard()`'s four rejection paths now use `location.replace`. Deliberate user
navigations — "View as member", the two `history.back()` helpers — still push, because a user's own
click belongs in their history.

`header_actions.test.mjs` **+8 (1379 → 1387)**, each with a negative control that mutates the real
source: re-adding an admin href to the real NAV, adding `tournament.html` (which needs its own arm
because it does not match `/^admin[-.]/`), and restoring the `roles[0]` fallback. **No migration,
no new route, no new page.**

**Recorded, not built** (build-bias rule): roadmap **v1.5** adds **§-1d**, the navigation and
program-scoping program in the owner's verbatim words (N-1…N-6 plus three questions that must be
answered before anything is built), and **§-1e**, the security and penetration-test track, opening
with **finding S-1: two ungated `/api/admin/*` routes** rather than an empty heading. Three
further N-1 neighbours (N-1c rail links out to member pages · N-1d soft lockout when `/api/me` is
slow · N-1e the admin shell paints before it is authorised) are registered, not chased.

**Iteration 19 (no version bump — `worker/scripts` and `worker/test` only, neither ships to the
runtime): the Pages half of the release ritual got a check that can fail.** Three files:
`worker/scripts/preflight.mjs` v1.1 (a `pages` check + the pure `pagesVerdict`, reusing
`sweep-buster`'s own `versionFromIndex`/`bustersIn` rather than parsing the version a second way —
C14), `worker/test/preflight.test.mjs` (+5, suite 1374 → **1379**, file count unchanged at 85), and
`RALPH.md` **v3.1**. The negative controls mutate the real input: **NC-8** rewrites the real
`web/index.html` busters to `0.98.0` — the exact state Pages is in — and **NC-9 feeds the guard the
real repo-root `index.html`, the page the old ritual actually fetched, and asserts it can never
report `ok`.** The defect, as an assertion. See §0a for what it found.

**v0.100.0 — §-1b W-G, the fixture carries the owner's real registration shapes.**

### The recorded blocker was false, and one command disproved it

W-G was carried for **three sessions** as *"needs an interactive session — the Google Drive
connector is unavailable headless"*, and the previous handoff told this session to skip it on that
basis. **One `search_files` call proved Drive reachable.** The note had been true once and was
propagated forward on trust ever since. It also means **§5's "the remaining REVCO sheets need an
interactive session" was wrong** — the Spring sheet was read this session and the rest are readable
the same way.

Read: **2026 Spring REVCO 4's Tournament (Responses)** — 21 team rows, the same form as the
Valentines sheet already on file, plus a "2025 Dec Revco" folder left unread.

### The four shapes

The fixture is a product — the owner judges features against it, so when it is unrealistic they
cannot tell a broken feature from a broken fixture and reasonably assume the feature. Each shape
below is real, and each left a code path the seed could never reach:

- **A contact with NO email.** The "Emails of Teammates" cell is free text and rarely holds one
  address per player. With an address on every seeded row, `reachable` and `total` on the marketing
  overview **could never differ** — W-F's segments counted every contact reachable forever. Its id
  is **even on purpose**: every `contactOffset` is even, so captains are always odd ids and this
  person is only ever a team member. A registering captain always supplies an address.
- **`email-sent`** — the commonest real unpaid state (3 of 21 Spring, 4 of 29 Valentines). The
  registrations screen's Unpaid filter is `pending`/`email-sent`/`cash-pending` and the fixture
  produced only two of the three, so a third of that filter had never selected a seeded row.
- **`A/AA` alongside `BB/A`** — the only two labels the form offers. `tierOf` scores them 350 and
  250; **no seeded team occupied the 350 rung**, so no seeded comparison could span two tiers.
  "Block Party at A/AA" is lifted verbatim from the Spring sheet.
- **A team name containing a comma** — `Jarvis, Jork It A Lil`, real, and the shape that breaks a
  naive CSV split. Same class as the apostrophe in `Spike Lee's` that `q()` exists for.

Also corrected: a comment reading **"24 test people" over a 48-name list**.

`sandbox_real_shapes.test.mjs` **(+10, 1364 → 1374)**. **Every assertion proves the shape changes a
downstream answer**, not that a row exists — counting rows is what passed on the broken v1 seed.
A1 uses the exact `reachable` expression from `overview()`; A4 runs against the **real** `tierOf`
(exported for the purpose) rather than a copy of its regex table, and demands two distinct tiers.
Four negative controls each mutate the real seeded rows.

### The reusable finding

**A blocker is a claim, and claims decay. Re-test the blocker before you route around it.** The
"do not believe any document" rule was being applied to things being *built on* but not to the note
saying something *could not be built*. A recorded impossibility is the cheapest kind of claim to
re-check — one call — and it had been costing a queue item per session. **Apply the same check to
every remaining "blocked" line before treating it as blocked.**

### Previous session (v0.99.0), kept for context

**§-1b W-F — segments can target one event.** The engine, members screen and segments UI were all
already built and called; the gap was the join between them. `asEventId` coerces the string a
`<select>` posts, because a **dropped filter widens the segment to the whole org** rather than
emptying it. `marketing_segments_event.test.mjs` (+14). Deployed and verified live.

---

## §3. The tester round — state after v0.89.0

Unchanged. R1–R4 all fixed and live; a fresh cross-browser round is still owed (§5, D-3).

## §4. Owner answers of record — do NOT re-ask

**SETTLED 2026-08-08 — THE TOURNAMENT-INSIDE-A-LEAGUE QUESTION. It unblocks §-1d, which had been
blocked on it since 2026-08-06.** The answer is **BOTH shapes, and the discriminator is where the
roster comes from**: (A) a **league-night tournament** played during a league night by the players
already there — roster is **the league's own teams**, no separate registration, and the owner says
it **lives inside the League module**; (B) a **linked open tournament** announced separately that
**anyone registers for**, with the bracket built from **whoever attends** — an ordinary tournament
that merely links back. **So "is Tournaments a tab inside League or a frame beside it" is answered
both ways, by the event's roster source** — a frame designed around either shape alone would have
been wrong for half the real cases. **Still to ask before building it:** what marks an event as
Shape A (a `parent_league_event_id`, or a flag plus a league reference) — **do not invent the
column**, and read §-1c **D-5** first, because `standings.rank` is an event-wide pool finish with no
division filter and `createBracket` seeds from `ORDER BY rank`, which collides with "draw the
bracket from the league's teams". **N-6 ("no bracket without pool") is a separate, still-open
question.**

**SETTLED 2026-08-08 (the three §-1f questions, answered in full):**

- **The admin/member switch is a REAL PRIVILEGE DROP, not a UI filter.** Owner: *"yes agreed drop
  privileges."* An admin "operating as a member" must be refused by the SERVER. **v0.103.0
  established what that costs and it is not a front-end change:** both gates query
  `user_org_roles` by `user_id + org_id` at request time (`index.js:331-343`, `admin.js:21-32`) and
  **nothing reads a role carried on the session** — so an acting-role needs a session column
  (migration), `buildCtx` reading it, and **both** gate functions honouring it. A drop that only
  `requireStaff` honours leaves the four `requireAdmin` routes open to a "member";
  `authorization_matrix.test.mjs` is what catches that.
- **No passwords. Ever.** Owner: *"ok"* to keeping the platform passwordless (magic link +
  WebAuthn). **The password-strength request is struck.** Do not propose adding passwords again.
- **Login branding is PER-ORG** — which **settles S-1b: `GET /api/orgs` stays unauthenticated, and
  that is now recorded as deliberate rather than unexplained.** The sign-in surface needs org
  branding before anyone has signed in. (Narrowing the public payload is still a fair improvement
  and belongs to the branding build, not to a security sweep.)
- **Cross-org product scoping.** Owner: *"the subfinder can be universal amongst all the orgs,
  player library, inbox, etc — leagues and tournaments can be nestled per org."* **This redefines
  §-1e priority 3**, which had assumed `org_id` on every read and write with no exceptions. The
  named universals are sub-finder, player library and inbox; leagues and tournaments are per-org;
  **"etc" is not a specification and the rest must be listed before that sweep runs.**

All of v1.18 §4 stands unchanged (direct commit authorised · CHANGELOG never hand-edited · waiver
system closed · SafeSport/M25 dropped · Twilio frozen · 12-on-5 · brackets auto-advance · pool floor
8 games, pools 6–11 · top division holds at 8 · seeding is a starting point · two views, no login on
the display · QR stopped · a hand-placed bracket slot is HELD · degrade, never collapse · grep the
single claim · KOTC §4.1–§4.4 · test range 90000–90999 disposable).
`docs/2026-08-04_reference_kotc-live-run_v1_0.md` stays trust-order-topmost for KOTC.

**Settled 2026-08-06 (do not re-open):** Offers are an **external system**, W-E.3 dropped, tryouts
ENDS at the squad board · **B5: keep `localStorage`, default org 1**, with an unbuilt follow-up to
let a user choose their own default org.

**Settled 2026-08-05:** Subs (excluded from tournament seeding; leagues count wins and divisions
played, never losses → **W-I**) · "Won before" = placement within division + tier-weighted score,
persisted ELO "potentially" → **W-H** · tournaments standalone once begun · **Supabase free tier**
under consideration, nothing built toward it.

**Settled 2026-08-08 (iteration 29) — THE END-OF-LEAGUE TOURNAMENT.** Owner: *"We do a tournament at
the end of the leagues. It changes based on participants and timeframe available."* This is Shape A,
and **it needs no new column and no migration.** The tournament is a **bracket on the league event**,
not a second event linked back to it. Three facts carry it, all asserted in
`worker/test/league_bracket.test.mjs` rather than read: (1) `brackets.event_id` already attaches a
bracket to any event; (2) `generateBracketFor` never reads `events.type` — a type gate injected as a
negative control turned 6 of the 8 tests red; (3) the Brackets screen's picker already lists every
event. **The roster question answers itself:** `teams.event_id` means the league event's teams ARE
the league's teams, so "the roster is the league's own teams" is structural, not a lookup.
**Do not build `parent_league_event_id`.** A separate event would need its own `teams` rows — the
league roster copied — and every no-show would then have to be fixed in two places.

**Settled 2026-08-08 (iteration 31) — THE UNIT IS GAMES, AND THESE ARE THE OWNER'S NUMBERS.**
*"however do not use time as the core unit of measure."* Time is a boundary (**7–8 hours for a
tournament, 6 is fine with fewer teams**), never the measure. The measure is **games per team**:

- **Target: ~8 games × 25 points in pool play**, *"before cutting anyone."* `MIN_GAMES_PER_TEAM = 8`
  in `formats.js` is that floor and has been since v0.62.0 — **there is exactly ONE definition of it
  and nothing may restate an 8.**
- **If pool play delivers fewer (6 or 7), EVERYONE BREAKS** so the first bracket games top them up.
- **Bracket games are 1 to 25 normally; sometimes 2 to 21**, *"depending on how we break them."*
- **A best-of-3 match counts 2.25 games** — *"25% chance of it going to 3 games."* **The quarter is
  load-bearing and must never be rounded away:** 6 + 2.25 clears the floor, 6 + 2 does not.
- ***"We try to break everyone possible to give them as many games as possible."*** A field short of
  the floor is therefore NEVER told to cut itself. **Top 8 (settled in iteration 30) belongs to the
  TIME boundary only**, and the time boundary is not the verdict.
- **N-6 ANSWERED:** *"Please ensure brackets are scored by pool play, that is the whole point of pool
  play."* The entry-seed fallback is not removed — a bracket-only event legitimately needs it — but
  it now announces itself, because a bracket seeded from entry order looks identical to one that
  came out of a real finish.


**Settled 2026-08-08 (iteration 30) — THE TWO KNOBS, ANSWERED AND SHIPPED IN v0.108.0.**
Owner, asked which lever gives when time is short: **"top 8"**. Asked per-division or league-wide:
*"depends on sign ups, the goal is to get everyone sufficient games (so we can double games in pool
play if needbe). Generally, 1 bracket across the league. but if we do large number of teams that are
unrated, then we will split based on divisions if there is a large skill variation."*

**THE MIDDLE CLAUSE IS THE ONE THAT MATTERS AND IT IS EASY TO READ PAST.** The constraint is a
**floor on games played, not a ceiling on minutes used.** A director who is short on time is not
asking "does it fit" — they are asking "what do I cut", and the answer is the bracket, because pool
play is where everyone gets their games. So the estimate suggests **top 8** on an overrun and never
suggests trimming pool play; and on an underrun it reports the spare time as **another round of pool
play**, which is the owner's actual goal rather than slack.

**League-wide is the DEFAULT, not the only mode.** The division split is the exception, reserved for
a large unrated field with wide skill variation — so `standings`' lack of a division filter (D-5) is
**not a defect against the default path**, and the default draw stamps no division at all (asserted).
D-5 becomes real work only when the owner hits the exception case; it is not urgent.

**What was already solved and must not be rebuilt:** "changes based on **participants**" is done.
`buildTree` takes any n, gives byes to the top seeds and never generates a play-in (owner, 2026-08-03:
*"we try to avoid pigtails"*); `a_size`/`include_rest` split A and BB so a tenth-place team still
plays; every slot is drag-or-type overridable for forfeits and people who went home (v0.91.0).

## §5. Owed by the owner

- **NEW, iteration 31 — BEST-OF-3 IS AN ESTIMATE INPUT ONLY, AND THIS IS THE MOST LIKELY PLACE A HOLLOW FEATURE APPEARS NEXT.** The preview will tell a director that a best-of-3 first round reaches the 8-game floor, and running it is their action at the scorer table — but the generator writes ONE match per bracket node and does not schedule the extra games. Should it? If yes, `matches.game_number` already exists and the honest shape is N rows per node, not a flag.
- **NEW, iteration 31 — THE 7–8 HOUR CEILING IS A KNOWN NUMBER AND NOTHING USES IT.** Owner: *"We try to finish tournament within 7 to 8 hours (6 is ok with less teams)."* The preview only knows the minutes a director types into a box; nothing models a whole-day budget across pool AND bracket together. Worth building, or is the typed box enough?
- **DONE 2026-08-08 (iteration 32) — the five superseded docs are DELETED with the owner's explicit OK.** docs/ went 21 → 16 `.md` files. INDEX.md now carries one deletion record in place of four rows. Historical citations in `CHANGELOG.md`, `LOOP.md` and `docs/archive/` were LEFT INTACT deliberately — dated records, not navigation.
- **NEW, iteration 32 — POOL TIME IS NOT MODELLED, so the 7–8 hour day cannot be checked end to end.** `estimated_minutes` covers the BRACKET only. Worth building?
- **ANSWERED 2026-08-08 and SHIPPED in v0.108.0 — both timeframe questions are CLOSED.** Which knob gives: **top 8**. Per-division or league-wide: **league-wide by default**, splitting only for a large unrated field with wide skill variation. Recorded in full in §4; **do not re-ask**. **D-5 is therefore NOT urgent** — it is the exception path, not the default one.
- **NEW, iteration 29, small and not blocking — bracket games get no clock times from the Brackets
  screen.** The server supports `slot_minutes` and will write real `starts_at` values, but
  `admin-brackets.js generate()` sends only `a_size`, `include_rest`, `points_to` and `courts`, so
  `times` is always null on that path. Worth one input if game times are wanted on the sheet.
- **Whether the next milestone is 1.0.0.** This release went v0.99.0 → **v0.100.0**, which is a
  normal 0.x increment and parsed cleanly everywhere. Declaring 1.0 is a product statement, not a
  build one. **NEW this session, not blocking.**
- **A tag vocabulary, or a decision that there isn't one.** Live D1: **0 of 49** org-1 contacts are
  tagged, so the segment tag filter matches nobody and only the signup widget ever writes one.
- **A region mapping for the area signal.** "N Co" has no code behind it and will not get an
  invented one; cities group on the literal stored string.
- **A fresh tester round on v0.89.0+, Edge and Chrome** (§-1c D-3). **UNBLOCKED — v0.101.0 is live
  on both pipelines.** Tryouts is complete end to end; the "Email these registrants" → segment path
  is the second thing to show; and **the shell ping-pong the owner reported is fixed in this
  release, so it is the first thing to re-test** (a hard refresh is needed — the old bundle is
  cached under the previous buster).
- **NEW 2026-08-06 evening, from §-1d — three questions that block the navigation program.** Ask
  once, do not guess. (1) **A tournament inside a league**: the owner wrote *"should potentially be
  available"* and *"it might be a sub category"* — that is thinking aloud, not a decision. Is a
  league-owned tournament (a) a tournament tagged to a league, drawing that league's teams, or (b) a
  separate tournament that merely links back? (2) **N-6, "we would not do bracket without pool"**:
  hard refusal, or a default the operator can override? The standing §-1b rule says default. (3)
  **N-2, "the screen with the pop up"**: which screen? The audit could not single one out with
  confidence, and guessing means fixing a modal the owner never meant.
- **NEW 2026-08-08, from §-1e S-3 — THE CROSS-ORG "UNIVERSALS" NEED A REAL DECISION, and the
  literal version would cost member privacy.** You said sub-finder, player library and inbox should
  be *"universal amongst all the orgs"*. **None of them is universal today — all three are
  deliberately org-scoped in code**, so this is an unbuilt feature, not a setting. Two consequences
  worth your explicit yes before anything is built: (1) **profile visibility was chosen inside one
  org.** Members set who can see their player card knowing the audience was that org; going
  universal republishes those cards to every other org's members. (2) **Blocks are org-scoped and
  would silently stop working.** Block someone in Boomtown and the block is stored against Boomtown
  only — go universal without re-scoping blocks and **the person you blocked reappears in the
  library and can message you from another org.** The sub finder also *emails* matching members, so
  universal means cross-org email fan-out. **Which of the three do you actually want universal, and
  do you accept those two consequences?** Nothing was built; v0.104.0 changed no scoping behaviour.
- **NEW 2026-08-08, from §-1e S-2 — the authorization questions v0.103.0's matrix produced. Ask
  once; these are JUDGEMENT calls the code cannot make.** Context first, because it ranks them:
  **live D1 holds 3 admin roles and ZERO staff roles across ONE user**, so every one of the ~190
  `requireStaff` gates currently admits exactly the single admin. **None of this is exploitable
  today. All of it activates the moment the first staff member is added.**
  (1) **S-2a — should `POST /api/admin/security/rescue-link` require ADMIN?** It is staff-gated
  today, and it issues a working sign-in link for **any** account on the platform, admins included,
  with no org or role restriction. In email mode the link goes to the victim's inbox (bad); in
  sandbox mode it is returned to the caller (account takeover). **Related and separately owed:
  is `BREVO_API_KEY` set on the live worker?** It could not be verified from here and it changes
  this finding's severity completely.
  (2) **S-2b/S-2c — `PERMISSIONS` declares `finance_export` and `crm_export` as admin-only, and
  both are enforced at staff.** Revenue CSVs and the whole marketing/CRM module answer staff.
  **Either the declaration is aspirational and should be corrected, or the gates should be
  tightened — but they must not keep disagreeing**, because the UI shows that table to users as
  the truth. Which way?
- **Whether to widen the shared admin type scale** — one change in `admin.css`, every admin screen.
  Owner's call, not urgent. (See also §-1c D-12, the registrations page's own hardcoded sizes.)
- ~~The remaining REVCO sheets need an interactive session~~ — **WRONG, corrected this session.**
  Drive is reachable headless. The Spring sheet is read; the **"2025 Dec Revco" folder** and the
  remaining tabs can be read the same way whenever they are wanted.
- Everything carried: eyes on the KOTC player screen / live-board animations / court board on real
  hardware · roster-sheet link (still 404) · `admin-kotc.html` type scale (.78 → .8rem) on the next
  `web/**` release · Chrome MCP go-ahead · minor/unknown-DOB session · C-1/C-2 confirms · member
  header smoke · Brevo/SPF/DKIM · Square production · VAPID secrets · `orgs.rules_url`.

---

## §6. Next build — the loop queue (RALPH.md v3.1 PHASE 2)

0. ~~**Land the deploy**~~ · ~~**§-1d N-1, the shell ping-pong**~~ — **BOTH DONE. v0.101.0 is live
   on BOTH pipelines** (§0b), each half sampled to convergence, `preflight` `deployed` PASS and
   `pages` PASS. Nothing is owed. **The tester round (§5) is unblocked and N-1 is the first thing
   to re-test.** Still run `node worker/scripts/preflight.mjs` at session start — it is one command
   and it is the check that was missing for two releases.
0a. ~~**§-1e S-1a + the gating ratchet**~~ — **DONE v0.102.0.** 102 of 102 admin routes gated,
   enforced every run. **§-1e priority 2 — the authorization matrix by role — is now the next unit
   in that track**: the ratchet proves a gate is REACHED, never that it refuses the right people.
   `requireStaff` accepts admin **or** staff and `requireAdmin` accepts admin only, and nothing
   checks that a route needing admin did not settle for staff. `admin.js` has both side by side and
   is where to start. **S-1b (`GET /api/orgs`, unauthenticated) is still open and still unasked** —
   it is probably deliberate (the sign-in surface needs branding); confirm who needs it before
   gating it.
1. **Let a user choose their own default org** (owner raised 2026-08-06). §6 called this "small,
   self-contained"; **iteration 19 verified the surface before designing and it is real work, not a
   missing screen.** Nothing exists — `grep -rn "default_org\|defaultOrg" worker/src web db/migrations`
   is empty. What was measured, so the next session need not re-derive it:
   - **`users` has no default column.** Live schema: `id, email, display_name, totp_secret,
     totp_enabled, created_at, updated_at, deleted_at`. **A migration 0043 is required** —
     `ALTER TABLE users ADD COLUMN default_org_id INTEGER REFERENCES orgs(id);` Apply via
     Cloudflare MCP **before** the push, read the ledger row back. **localStorage cannot do this
     job**: `bt_org` already persists per browser, so a local-only "default" is only ever consulted
     on a fresh browser — where it is equally absent. It has to be server-side to mean anything.
   - **`GET /api/me` is `me(request, env)` at `index.js:646-659`, NOT in a module**, and is matched
     *above* the `startsWith("/api/")` branch, so it never sees `ctx`. It returns
     `{ user:{id,email,display_name,totp_enabled}, roles:[{org_id,role}], passkeys:n }`. Add the
     column to that `SELECT` and it reaches the client with no new route.
   - **A `PUT /api/me/default-org` DOES get `ctx`** — the top-level match is the exact string
     `"/api/me"`, so sub-paths fall through to the dispatch table (precedent:
     `PUT /api/me/sub-availability`, `announcements.js:245`). `ctx = {session, orgId, orgOk, userId,
     role}`, so **`ctx.userId` is the id to write** — do *not* copy sub-availability's
     `contactForSession`, which resolves a `contacts.id`, not a `users.id`. `json`, `audit` and
     `isStaff` are injected into every module.
   - **THE HAZARD, checked before choosing a shape:** the decision point is `admin-nav.js:624-625`,
     and **`org_honesty.test.mjs:154-157` pins two literal source substrings there** —
     `orgs.some((o) => Number(o.id) === current)` and `location.reload(); return;`. A change that
     rewrites line 624 into a helper call reddens the suite *on a correct fix*. **The natural shape
     is safe**: keep line 624 exactly as it is and change only the `current = Number(orgs[0].id)`
     fallback inside the block to prefer `me.default_org_id` when it is in the role-filtered set.
     `header_shell.test.mjs:85-88` separately pins `api("/api/orgs")` — do not replace that fetch.
   - **UI placement, and it is not the header.** The admin header is **38 byte-identical copies**
     enforced by `header_shell.test.mjs` (`variants.size === 1`); adding one element means editing
     all 38. There is **no user-account page** — `admin-org-settings.html` is the *org's* identity.
     Cheapest honest home is a row on `admin-org-settings.html` ("Make this my default org" for the
     signed-in user, clearly separated from the org's own fields), or a new page, which moves
     `sync-rail` + NAV parity. **`tokens.test.mjs:141-146` is a CEILING at exactly 20/20 with zero
     headroom** — any new page-level `:focus-visible` rule with an `outline:` not routed through
     `var(--focus-ring)` reddens the suite immediately.
   - **The in-memory D1 shim is `worker/testkit/d1-memory.mjs`** — *not* `worker/test/`, and its own
     header comment names a path that does not exist. `DB.query(sql, ...params)` is **synchronous**
     and returns a **bare array**; `DB.prepare(sql).all()` is async and returns `{success, results,
     meta}`. **There is no `DB.all()`** — that mistake has opened six sessions in a row.
2. **§-1c D-11** — add `e.org_id = ?1` to the `played` filter in `marketing.js`. Verified to need
   **zero test changes** (`?1` is numbered and consumes no bind). Cheap rider on any release
   touching that file.
3. **W-H** (persisted ELO — owner-gated, "potentially") and **W-I** (league sub record).
4. **§-1b W-G follow-on, now that Drive is known-reachable:** the "2025 Dec Revco" folder and the
   remaining tabs are readable headless. Only worth doing if a specific question needs them —
   the four shapes the fixture was missing are now in it.
5. Then the v0.88.0 §6 remainder: net-empty redistribution · bracket disputes (owner-gated) · points
   cap + net labels (migrations, apply via MCP first) · roadmap §0 inventory · the D-4 remainder
   (LFG moderation, programs CRUD, POS sale detail, staff shifts, subs board, pass reverse/grants,
   facility check, divisions/assign, matches/*/court).

---

## §7. NEXT SESSION — start here

**v0.102.0 is fully live — worker AND Pages, both sampled to convergence. Nothing is owed from the
last session.** Confirm it in one command, which checks both halves:

```bash
node worker/scripts/preflight.mjs
```

Expected: suite **1399/1399** (measure it, never project it) · **86** test files · **51** modules ·
D1 ledger **0042**, 42 files in `db/migrations/` · buster a single value at **0.102.0**, 400
occurrences across 64 files · D-4 baseline **15** · 0 uncommitted, 0 unpushed · `deployed` **PASS**
· `pages` **PASS**. Any mismatch is the session's first problem, not a footnote. Preflight still
cannot self-verify two of its own checks — settle them by hand: `git fetch && git log
main..origin/main` empty, and a live `schema_migrations` read via Cloudflare MCP (last read:
**MAX=42, 42 rows**). **"Could not be verified" is not "passed".**

**Still true before you bump:** run **31118355010** renders `queued` on old sha `b5d0322` — a zombie
with an empty jobs list and a frozen `updated_at` that `gh run cancel` refuses as "completed". If it
ever executes it deploys **v0.100.0** and its own parity check passes, which is a silent rollback.
**The defence is the convergence sampling you already run.** v0.102.0's health flapped to v0.101.0
three times before settling — never to v0.100.0, which is exactly the discrimination that sampling
exists to make. If a post-bump sample reads v0.100.0, suspect this before a Cloudflare flap.

**BUILD: §-1e priority 2 — the authorization matrix by role.** v0.102.0's ratchet proves every one
of the 102 admin routes REACHES a gate. It says nothing about whether the gate refuses the right
people, and it says so in its own header.

- **The real question.** `requireStaff` accepts **admin OR staff**; `requireAdmin` accepts **admin
  only**. Both are in use and **nothing checks that a route needing admin did not settle for
  staff.** `admin.js` has both side by side — `listUsers`/`addUser`/`setRole`/`revokeRole` take
  `requireAdmin`, `listMembers`/`patchMember` take `requireStaff` — so it is the place to start and
  the place where a wrong answer would matter most.
- **VERIFY BEFORE DESIGNING, and expect this one to come back different.** Seven of the last nine
  W-units found the engine built and only the screen missing; this is not that shape. The likely
  finding is a *judgement* question — which routes deserve admin — and that may be an owner
  decision rather than a defect. **Read the routes first and say what you found before proposing a
  rule.** If it turns out to be a policy question, it belongs in §5 as an owner question, asked
  once, not guessed.
- **The shim is `worker/testkit/d1-memory.mjs`** — its API is **`DB.query(sql, ...params)`**,
  synchronous, returning a **bare array**. `DB.prepare(sql).all()` is the async form. **There is no
  `DB.all`** — that has opened the first red six sessions running (§-1c D-14).
- **Assert call sites, never definitions.** This track has now paid for that rule twice: once in the
  original false "clean", once in v0.102.0's own first run.

**S-1b is still open and still unasked.** `GET /api/orgs` (`index.js:363` → `listOrgs(env)`) is
unauthenticated and lists every active org. It is **probably deliberate** — the sign-in surface
needs org branding before anyone is signed in, and `header_shell.test.mjs:85-88` pins the admin
switcher as a caller. **Confirm who needs it unauthenticated before gating anything.** Do not fold
it into another unit.

**§-1c D-11** rides free on any release touching `marketing.js`: add `e.org_id = ?1` to the `played`
filter. `marketing.js:92` is the only site (`any`/`none` never join `events`), `?1` consumes no
bind, so `marketing.test.mjs:36` stays green. **v0.102.0 did not touch that file, so it was
correctly not taken** — the rider is conditional, not a to-do.

**Standing rules:** sqlite_master before any NEW table · numbered SQL in `db/migrations/` only ·
migrations via Cloudflare MCP **before** the push, ledger row read back after · bump →
`sweep-buster.mjs --write` → `sync-rail.mjs --write` → preflight, in that order · docs-only gets no
bump and no sweep, but any release that bumps MUST sweep · every guard ships a negative control that
**mutates the real input** · assert call sites, never definitions, in both directions · byte-verify
the `index.js` bump as a one-line diff · **confirm BOTH halves of the deploy and sample to
convergence** · `git commit`/`git push` have gone first-try for two releases running — retry up to
three, but do not assume three are needed · **handoff §4 owner answers are settled — re-asking one
is a defect.**

**If a deploy misbehaves:** `/api/health` proves the **worker only**. Pages is a separate,
GitHub-managed pipeline with **no `workflow_dispatch`** — the only lever is a push to `main`. Two
releases running, one push created both runs and both went green in about a minute; that is the
healthy baseline. During the 2026-08-06 outage a push touching `worker/**` produced **no run at
all** (run *creation* was degraded), and `gh workflow run deploy-worker.yml` then worked first try.
**Do not debug the paths filter when a push goes quiet mid-incident — dispatch.**

**Owed by the owner** (§5, ask once, do not guess). **THREE §-1d QUESTIONS BLOCK THE ENTIRE
NAVIGATION PROGRAM** and must be asked before designing around them — v0.98.0's lesson is that an
owed question can DELETE work rather than unblock it: (1) a tournament inside a league — tagged to a
league and drawing its teams, or separate and merely linked? The owner wrote *"should potentially be
available"*, which is thinking aloud, not a decision. (2) N-6 *"we would not do bracket without
pool"* — hard refusal or overridable default? The standing §-1b rule says default. (3) N-2 — which
screen is *"the screen with the pop up"*? The audit could not single one out. **Also owed:** whether
the next milestone is 1.0.0 · a tag vocabulary, or a decision that there isn't one · a region
mapping for the area signal · whether to widen the shared admin type scale · **a fresh tester round
on Edge and Chrome — unblocked, and the shell ping-pong fixed in v0.101.0 is the first thing to
re-test (hard refresh required; the old bundle is cached under the previous buster).** B5 and "what
an offer is" were answered 2026-08-06 — do not re-ask. **The REVCO sheets do not need an interactive
session; Drive is reachable headless.**

**And above all:** do not believe any document about anything you are about to build on — including
documents that say a thing cannot be done, and **including a check that reports clean**. Ask what a
passing check would look like if the thing it guards were broken; if the answer is "identical", it
is not a check. **v0.102.0 is the worked example in both directions**: the guard it replaced
reported clean by reading a definition instead of a call site, and the guard that replaced it caught
a false positive in its own first output — which was only found by reading the code it accused. One
check, not a sweep; if the claim is about data, query live D1, then read the code that writes it.
