# Boomtown Platform — Session handoff

**File:** `docs/2026-08-06_handoff_v0_97_0.md` · **Version:** v1.23 · **Created:** 2026-08-06
**Status:** ACTIVE — state of record. **END-OF-SESSION handoff: this session is closed, start a
fresh chat from §7.**
**Supersedes:** `docs/2026-08-05_handoff_v0_96_1.md` (v1.22), **renamed in this commit** — one live
handoff, the history in git. By rename it also supersedes the v0.89.0–v0.94.0 handoffs.
**Read with:** `CLAUDE.md` §0 read-order → standards → roadmap (**§-1 first, then §-1b**) → INDEX.
**Loop:** the build runs as a /loop-driven ralph loop — `RALPH.md` v3.0 is the instruction set,
`LOOP.md` the ledger (read its last three entries, not the whole file).

---

## §0. Delivery

**ONE release shipped and verified live this session — v0.97.0**, sampled to convergence. Working
tree clean, nothing unpushed. **Nothing is half-built.**

**The loop prompt was STALE for the second session running** — it expected v0.94.0 and asked for W-D,
which iterations 11–12 shipped two releases earlier. Preflight named the mismatch on the first
command, `LOOP.md` explained every measured number, and the session moved to the genuine next unit.
That is the prompt's own rule working: **any mismatch is the session's first problem, not a footnote.**

**Two of the four are design fixes caught AFTER their feature shipped** (v0.95.1, v0.96.1). Both were one-declaration CSS corrections and both still needed a full release, because a `web/**` change without a version bump means the sweep never runs and every warm cache keeps the old stylesheet. Budget for that: a design review is not free, and finding the defect is the cheap half.

**v0.96.0's sampling flapped visibly** — v0.95.1, v0.96.0, v0.95.1, v0.96.0, then five in a row.
That is the ~2-minute edge propagation the rule describes, and it is the concrete argument against
"fetch once and call it deployed": a single sample at the wrong second reports either version.

**v0.95.1 exists because a design review found a real defect in v0.95.0's panel after it shipped**
(§2). It is a two-declaration CSS change plus one assertion, and it needed a full release rather than
a quiet edit for one reason worth remembering: **a `web/**` change without a version bump serves
stale CSS from cache.** Any touch to `web/` is a release — bump, sweep, push, verify.

Release rules, now proven eight consecutive times: preflight CLEAR → CHANGELOG entry **into** the
release commit → commit → push, ONE push. The health fetch is the real check and it **flaps for
~2 minutes** across edge locations, so sample until five consecutive reads agree. **If `git push` is
refused by the classifier, retry — it has now succeeded on the third attempt twice.** Never sweep the
buster with `sed` (`node worker/scripts/sweep-buster.mjs --write`).

**The health URL is `https://boomtown-api.vvisuth.workers.dev/api/health`** — take it from
`worker/scripts/preflight.mjs:58`, not from memory. This session burned eight minutes sampling a
guessed `api.boomtownathletics.com`, read 40 straight connection failures, and nearly filed "the
sandbox has no network" as a finding. It had network. **When a fetch fails uniformly, check the
hostname against the repo before concluding anything about the environment.**

## §1. Build state — measured 2026-08-06 (end of session)

| Thing | Value | How checked |
|---|---|---|
| Repo `main` and `/api/health` | **v0.97.0**, agreeing | preflight `deployed` + live sample to convergence |
| Test suite | **1338 passing, 0 failing** | measured. 1312 → 1323 → 1324 → **1338** |
| Test files | **82** | F-37 parity gate |
| Worker modules | **51**, all parse | preflight `syntax` |
| D1 ledger | **0042**, 42 files in `db/migrations/` | read live via Cloudflare MCP this session: `MAX(version)='0042'`, 42 rows |
| Cache buster | single value **0.97.0**, **400** occurrences, **64** files | `sweep-buster.mjs` |
| D-4 uncalled baseline | **16** (− four squad routes, struck v0.97.0) | `route_reachability.test.mjs` |

---

## §2. What shipped this session

**v0.97.0 — §-1b W-E.2, the tryout squad board.** Four routes (`squads` GET/POST, `squads/:id`
PATCH/DELETE, `squads/:id/assign`, `squads/:id/remove`) had been built, tested and org-scoped **with
no caller anywhere**. Verified before designing — the fifth consecutive W-unit where the engine was
whole and only the screen was missing, and the fifth time that check cost one measurement.

New page `admin-squads.html` + `admin-squads.js`, rail entry "Tryout Squads":

- **Two taps, no dragging.** Pick a player, pick the team. Assign MOVES an already-placed player, so
  a move is also two taps. Drag is the pool board's answer and the wrong one here — a director does
  this holding a phone at the side of a court, and drag has no keyboard path that is not a worse one.
- **Every listener is delegated and attached once, at boot.** `#sqUnplaced` and `#sqGrid` have their
  innerHTML replaced per render but are never recreated, so a listener added in a render accumulates
  for the life of the page. That is §-1c **D-6**, and this page does not inherit it.
- **The server owns "short" and "full".** `squadNeeds()` says full = headcount met AND no position
  short; the client renders that verdict and does no arithmetic of its own.
- Both pages carry the tryout across in the URL, so moving between evaluations and squads never
  costs a re-pick (req #19).

`tryouts_squads.test.mjs` (+14), three negative controls each mutating the real shipped client.
**D-4 baseline 20 → 16** — four struck at once because they are one surface. `tryouts/*/card/*`
stays: staff card correction is now **W-E.2b**, split out rather than bundled.

### Previous session (v0.95.0–v0.96.1), kept for context

**Three releases: v0.95.0 — §-1b W-D, seeding suggestions · v0.95.1, the design fix W-D needed ·
v0.96.0 — §-1b W-E.1, the director's tryout roll-up.** W-D was built from
`docs/2026-08-05_spec_seeding-suggestions_v1_0.md` rather than re-derived, which was the right call:
the spec's four corrections all held up, and re-deriving them would have cost the session.

### v0.96.0 — §-1b W-E.1 (the director's half of tryouts finally has a screen)

**The verify-first method paid for the fourth consecutive unit.** `GET /api/admin/tryouts/:id/summary`
has been built, tested and org-scoped **since v0.60.0 with no caller anywhere**, and the page's
"Director summary" control was an `<a href>` pointing at `admin-buildstatus.html#tryout-N` — a page
about which modules exist, not about this tryout. **Failure class 1 with a button on top of it**, and
precisely what the owner's *"try out page does not work … no test data here or form to use"* meant.

Built the screen only: the roll-up is a **view on the same page** rather than a second page — same
tryout, same event picker, one tap either way (req #19). Every evaluated player with offers, noes,
coach count, **rating range** and "2/3 offer"; every column sorts from its header; a second click
reverses; count columns open **descending** because a director opening "Offers" wants the most-wanted
first. Sort state is written to `aria-sort` and shown as an **arrow character**, never colour alone.

**The rating is a range and nothing averages it.** `rollUp`'s own comment says it: *"Range, not mean.
Two coaches at 2 and 5 is the interesting case, and a mean of 3.5 erases it."* The screen does no
arithmetic on the two ends. This is the property most likely to be "tidied up" later, so **NC-1
mutates the real shipped client to compute a mean and proves the guard catches it.**

Coach privacy is untouched — the evaluating cards still show one coach their own work only, enforced
in SQL — and the guard asserts **both halves against the same fixture**, so "the roll-up aggregates
everybody" is not a claim about an empty set. An unevaluated player is **absent** rather than a row of
zeroes, because 0/0 reads as a verdict nobody gave.

`tryouts_rollup.test.mjs` **(+11, 1312 → 1323)**. **D-4 baseline 21 → 20**, demanded by the ratchet
the moment the caller landed.

**Deliberately not built: the other five tryouts routes.** The squad board (`squads` GET/POST,
`squads/:id` PATCH/DELETE, `assign`, `remove`) and staff card correction (`card/:contactId`) are a
second, drag-and-drop-shaped screen. Half-building two screens is worse than finishing one, so they
are **W-E.2** with the pool board as their working precedent. A third piece, **W-E.3**, is the owner's
"send offers from the table": **there is no route for it and none was invented** — a per-coach
`verdict` is a private judgement, not an offer, and what an offer *is* needs an owner answer first.

### v0.95.1 — the panel stops shouting over the board it advises

A design review after v0.95.0 shipped flagged the panel's **3px gold left edge**. It was a real
defect, and the cliché ("a thick coloured border down one side of a card") is the lesser half of why.
**The larger half is hierarchy: it spent the loudest device on the page on the page's least important
element.** The pool board's signature is the tiles a director drags; standards §5 says spend boldness
in one place and keep everything around it quiet, and that edge did the opposite of the panel's own
stated intent, which was to read as part of the board.

Fixed by borrowing `.pb-pool`'s container **exactly** — same `1px solid var(--border)`, same radius
token — and marking the panel with the dot idiom **the page already uses** for unsaved state
(`.pb-state.dirty::before`) in `--emphasis`: navy on light, gold on dark, AA in both. Gold stays never
ink.

**The guard was the other half of the defect, and this is the reusable part.** A9 asserted the literal
string `border-left: 3px solid var(--accent)` — a pinned *implementation*, not the rule — so it went
red on the correct fix. That is exactly the class `divisions_page.test.mjs` v1.1 recorded one release
earlier when its first draft demanded `role="alert()"`, and the resolution is the same: **when a guard
reddens on a deliberate change, ask which of the two is wrong before touching either.** Here it was
the guard. It now asserts the invariant — no `.pb-sug` rule paints text with `var(--accent)` or a
literal gold hex, the panel's container matches `.pb-pool`'s, and no `.pb-sug` rule carries a thick
single-side accent border — and it **counts the rules it matched against a floor of five**, so a
selector rename cannot make it pass by matching nothing. Proved to fail in both directions before
shipping: re-inserting the edge flips the slab check, and swapping the dot's `--emphasis` for
`--accent` flips the gold-ink check.

Behaviour is untouched: two CSS declarations, one class attribute, one assertion. Suite stays
1312/1312.

### The session's most reusable finding

**A guard's own comment tripped the guard for a fourth time in three sessions — and this time it was
a comment I wrote in the same hour.** `board_suggest.js` explains that it invents no "N Co" region
taxonomy; the test asserting there is no invented "N Co" taxonomy read the file including comments
and went red. The same shape hit a second check in the same file (a comment saying "there is no
'no suggestions' state" tripped the no-empty-state-copy check).

The habit that actually prevents it: **when a guard asserts what code DOES, strip comments at the
top of the file, once, into a named constant, and give the stripper a control in both directions** —
prove the phrase is present in the raw text and absent from the stripped text, and that the code
survived. `board_suggestions.test.mjs` does this with `MOD_CODE`. Asserting over raw source is the
default that keeps failing, and it fails *silently in the other direction* too: a guard that greps
raw source for a rule can pass because a comment mentions the rule.

**Second finding, on fixtures:** three assertions in the first draft of the guard passed or failed
for a reason the test never named, because one fixture person held *two* claims — a champion who had
also placed first at an older event, and a future-event winner who had also placed third in the past.
**A test about one mechanism needs a fixture subject whose only claim is that mechanism.** Two extra
contacts fixed it, and A4b/A4c/A3 then failed honestly and were fixed for real.

### v0.95.0 — §-1b W-D (seeding suggestions: proposals, never rules)

**What the owner settled, and how it changed the build.** Both spec §7 questions were asked once and
answered, and the second answer widened the design:

> **Subs** — *"Subs for tournaments, non issue we wont update as we dont have subs for that usually.
> For leagues yes, and we can count the number of wins they have as subs on their record (and
> divisions played). The team doesnt record doesnt matter if they have subs, but they should be noted
> if they get subs regularly to admin."*

So W-D excludes `is_sub` rows entirely, and the league-side sub record became **roadmap §-1b W-I** —
count wins and divisions played, never losses, flag a team that takes subs regularly.

> **"Won before"** — *"We should rank every team, so depending out of bracket, from pool they should
> count all teams 1-X for each division. Then simply list their placement for divisions. We can award
> a score/number as there is minor overlap between bottom of A and top of BB, similar to ELO score.
> Modidified since this is a team sport. - rank includes Open/AA - A - BB - B - Recreational. I would
> just want their placement within their division and estimated score total then convert into ElO
> number for raning potentially."*

So a team's history is **placement within its division plus a tier-weighted score**: tiers 100 apart
(Open/AA 400 · A 300 · BB 200 · B 100 · Recreational 0, plus the mixed labels the real registration
form uses — `A/AA` 350, `BB/A` 250, `B/BB` 150), placing scored 0–100 inside a tier. **The bottom of A
and the top of BB therefore come out equal by construction**, which is the overlap the owner
described, derived rather than tuned. An unrecognised division label earns **no** tier and is left out
of the strength comparison rather than guessed at. A *persisted* ELO rating is **roadmap §-1b W-H**,
unbuilt on purpose: the owner said "potentially", and it needs a ratings table this release does not
add.

**Built:** `worker/src/board_suggest.js` (read-only, single export) + one key on `loadBoard`'s return
+ a panel on `web/admin-pool-board.html` + `board_suggestions.test.mjs`. **No new route, no
migration.** The board POST already spreads `...board`, so the panel refreshes on every save with no
second request.

Four signals, each in its own `try/catch` returning `[]`: **`spread_winners`** (division winners
bunched in one pool) · **`spread_strength`** (one pool finished higher than another, stated in
*divisions' worth* — the internal score is a unit nobody has been taught and never reaches the
screen) · **`split_repeat`** (two teams in a pool whose players keep sharing rosters, counted once per
shared roster, not once per pair of people) · **`spread_area`** (one city bunched in one pool; captain
city, literal stored string, **no region taxonomy**, so "Fort Collins" and "Ft Collins" stay two
groups and the signal understates).

**The three product rules, each asserted:** the 6–11 preference suppresses a *suggestion* and never a
save · an unsatisfiable suggestion is dropped **in silence**, with no "not enough history" line
anywhere · **no sentence names a person**, though every signal resolves identity through people.

**The panel** sits between `#pbBoard` and `.pb-workspace` — outside `#pbBoard`, which `render()`
rewrites and `fail()`/`orgEmptyState()` replace wholesale. Drawn from `ingest()` only, never from
`render()` or `wire()`, with one delegated listener on a static node, because `wire()` already stacks
drag handlers on the never-recreated `#pbWork` (§-1c D-6). Rows carry **Show me** (rings the teams,
clears itself after 4s) and **Dismiss**. Visually it is the board's own furniture one register
quieter, with a gold left edge as a *rule* — never gold ink.

**The guard (+22, 1290 → 1312).** The fixture forces all four kinds, because a no-write assertion over
a payload that proposed nothing is vacuous. `rank` is written to **disagree** with pool play wherever
the two can be told apart, so a module reading `rank` fails here: a team holding `rank=1` that placed
last and lost its final must not be credited, while a team that placed third and won a *scored* final
must be. Ties credit nobody; a **drawn** final credits nobody (without `score_a <> score_b` a tie
crowns `team_b` through the `ELSE` branch); a future-dated `in_progress` event is not history. The
no-write assertion **records every prepared statement**, so a no-op `UPDATE teams SET pool_id=pool_id`
is caught — NC-3 proves that detector fires by inserting exactly that into a copy of the real module.
NC-1 renames a real column and asserts the two history signals go silent **while the signal that
never touched that column still answers**, which is the per-signal isolation being demonstrated rather
than asserted. A recorded limit: a **whitespace-padded** email does not resolve to a contact, because
wrapping the `contacts` side of the join in `trim()` would defeat `idx_contacts_org_email` for every
lookup — A6b pins that trade so it cannot be reversed in one direction only.

**Live-data reality check, measured this session:** live D1 has exactly **one** past event (Spring
Slam, 4 teams, **zero divisions**) and five future-dated ones. So on real data today the tier-weighted
strength signal produces **nothing** — there is no past event with a division to read. That is correct
behaviour, not a defect, and it is why the guard's fixture had to manufacture the history.

---

## §3. The tester round — state after v0.89.0

Unchanged from v1.18. R1–R4 all fixed and live; a fresh cross-browser round is still owed (§5, D-3).

## §4. Owner answers of record — do NOT re-ask

All of v1.18 §4 stands unchanged (direct commit authorised · CHANGELOG never hand-edited · waiver
system closed · SafeSport/M25 dropped · Twilio frozen · 12-on-5 · brackets auto-advance · pool floor
8 games, pools 6–11 · top division holds at 8 · seeding is a starting point · two views, no login on
the display · QR stopped · a hand-placed bracket slot is HELD · degrade, never collapse · grep the
single claim · KOTC §4.1–§4.4 · test range 90000–90999 disposable).
`docs/2026-08-04_reference_kotc-live-run_v1_0.md` stays trust-order-topmost for KOTC.

**New this session, and settled:**

- **Subs.** A non-issue for tournaments — excluded from seeding entirely. For **leagues**: count a
  sub's wins and divisions played, never their losses; a team's own record is unaffected by using
  subs; flag to staff a team that takes subs regularly. → §-1b **W-I**.
- **"Won before" = placement within a division plus an estimated score**, with the ladder
  Open/AA · A · BB · B · Recreational and deliberate overlap at the tier boundaries. A true persisted
  ELO number is **"potentially"** — wanted eventually, not now. → §-1b **W-H**.
- **Tournaments are standalone once begun.** Initial seeding may read previous win data; after that
  the tournament does not depend on history. Keep a history of placement and add to each group over
  time. The owner is considering **Supabase free tier** for live in-tournament data feeds — recorded,
  not decided; nothing has been built toward it and the current stack is unaffected.

## §5. Owed by the owner

- **A decision on B5** — `bt_org`: keep `localStorage` (now self-healing) or move to
  `sessionStorage`. Recommendation: **keep localStorage**. Reversible. **Not blocking anything.**
- **What an "offer" IS** — the only thing blocking §-1b **W-E.3**, and the one place the tryouts
  workflow stops. A coach's `verdict` is a private judgement, not an offer. Turning the director's
  roll-up into an offer a player receives needs an answer first: is it an **email**, a **place on a
  squad** (which the squad board now does, so W-E.3 may already be half-answered), or a **status on
  the contact**? No route exists and none was invented. Asked once, here.
- **A region mapping for the area signal.** The owner's "N Co" shorthand has no code behind it and
  will not get any invented one. Cities group on the literal stored string, so "Fort Collins" and
  "Ft Collins" are two groups today and the signal quietly understates. A list of
  *region → member cities* is all it needs.
- **A fresh tester round on v0.89.0+, Edge and Chrome both** (§-1c D-3). Seven releases since the last
  round; R1–R4 are all fixed and live. Expect the stale-cache symptoms to clear on the first
  navigation — **whatever survives that is real and new.**
- **The REVCO registration sheets.** The owner pasted one Valentines REVCO export this session
  (29 real team rows). It is recorded in §6 as W-G's input; the remaining sheets live in Google Drive
  and **need an interactive session** — the Drive connector is unavailable headless.
- Everything carried: eyes on the KOTC player screen / live-board animations / court board on real
  hardware · roster-sheet link (still 404) · `admin-kotc.html` type scale (.78 → .8rem) on the next
  `web/**` release · Chrome MCP go-ahead · minor/unknown-DOB session · C-1/C-2 confirms · member
  header smoke · Brevo/SPF/DKIM · Square production · VAPID secrets · `orgs.rules_url`.

---

## §6. Next build — the loop queue (RALPH.md v3.0 PHASE 2)

1. **§-1b W-E.2b — staff card correction. Do next, and it is small.** The LAST uncalled tryouts
   route: `PUT /api/admin/tryouts/:eventId/card/:contactId`, a form over `CARD_COLS` (`positions,
   age_groups, height_cm, prev_club, jersey_size, player_note`) so staff can fix what a player typed
   at registration. **Built and tested already — confirm in one grep, then build only the form.** It
   belongs on the evaluate card in `admin-tryouts.html`, not on the squad board. Note that
   `tryouts_rollup.test.mjs` pins three literal SPELLINGS of that page's view toggle (lines ~104,
   232–235, 255) — if a change reddens them, the guard is what is wrong, not the change.
   **W-E.3 "send offers" stays blocked on the owner**, not on code: there is no offer route, a
   per-coach `verdict` is a private judgement rather than an offer, and what an offer *is* (an email,
   a squad place, a status on the contact) must be answered before anything is designed.
2. Then **W-F** members/comms · **W-G** Drive-shaped seed (**needs an interactive session**).
   **W-G's real input is the owner's Valentines REVCO export** — columns `Timestamp · Email ·
   Captain's Phone · Team Level (BB/A, A/AA) · Team Name · Captain's Name · Team Mate 2/3/4 Name ·
   Emails of Teammates · Instagram · waiver · Payment Selection · Payable Order ID · Total · Status
   (paid / created / refunded / checkout-started / paid (TEST)) · Method · Transaction ID · Updated`.
   The shapes that break a naive importer: teammate names with **no matching email**, one email
   covering a whole team, `"N/A (Searching)"` and `"I don't know them"` as literal cell values, the
   **same captain registering two teams at two levels**, and a `Team Level` vocabulary (`BB/A`,
   `A/AA`) that is exactly what W-D's tier map already reads. **Four of the 29 rows never paid**, so
   a seed built from this must carry unpaid registrations, not only clean ones.
3. Then **W-H** (persisted ELO — owner-gated, "potentially") and **W-I** (league sub record).
4. Then the v0.88.0 §6 remainder: net-empty redistribution · bracket disputes (owner-gated) · points
   cap + net labels (migrations, apply via MCP first) · roadmap §0 inventory · the D-4 remainder
   (LFG moderation, programs CRUD, POS sale detail, staff shifts, subs board, pass reverse/grants,
   facility check).

---

## §7. NEXT SESSION — start here

> Continue the Boomtown Platform build in `D:githubepostplatform` as a ralph loop:
> `RALPH.md` v3.0 is the instruction set, `LOOP.md` the ledger — read its last three entries and
> nothing more.
>
> FIRST: `node worker/scripts/preflight.mjs` — do not proceed until it prints CLEAR. Expected: repo
> `main` and `/api/health` both **v0.97.0** · 0 uncommitted, 0 unpushed · suite **1338/1338 passing
> (MEASURE it, never project it)** · **82** test files · **51** modules · D1 ledger **0042** and 42
> files in `db/migrations/` · cache buster a single value at 0.97.0, **400 occurrences across 64
> files** · `route_reachability` D-4 baseline at **16**. Any mismatch is the session's first problem,
> not a footnote — the last two prompts were stale and preflight caught both.
>
> Read `docs/2026-08-06_handoff_v0_97_0.md` (v1.23, state of record — §2 for what shipped and the
> session's reusable finding, §4 for settled owner answers, §5 for what is owed) →
> `docs/2026-07-30_standards_v2_0.md` → `docs/2026-08-02_roadmap_v1_0.md` (§-1b governs the queue;
> §-1c is the deferred-defect register) → `docs/INDEX.md` (§4's standing-rules table is the part to
> actually read).
>
> BUILD §-1b **W-E.2b — staff card correction**, the last uncalled tryouts route:
> `PUT /api/admin/tryouts/:eventId/card/:contactId`, a form over `CARD_COLS` (`positions, age_groups,
> height_cm, prev_club, jersey_size, player_note`). **It is already built and tested — confirm in one
> grep, then build only the form.** It belongs on the evaluate card in `admin-tryouts.html`, not on
> the squad board. **Warning:** `tryouts_rollup.test.mjs` pins three literal SPELLINGS of that page's
> view toggle (~104, 232–235, 255). If your correct change reddens one, the guard is what is wrong —
> rewrite it to the invariant, and give it a negative control that mutates the real input.
> W-E.3 "send offers" is BLOCKED on the owner; do not invent an offer route.
>
> Load `/emil-design-eng` then `frontend-design` before any UI. `ux-copy` is NOT installed —
> standards §8 is the rule set.
>
> Standing rules: sqlite_master before any NEW table · numbered SQL in `db/migrations/` only ·
> migrations via Cloudflare MCP BEFORE the push, ledger row read back after · bump version →
> `sweep-buster.mjs --write` → `sync-rail.mjs --write` → preflight, in that order · docs-only gets no
> bump and no sweep, but any release that bumps the version MUST sweep · every guard ships a negative
> control that MUTATES THE REAL INPUT · assert call sites, never definitions, in BOTH directions ·
> byte-verify the `index.js` bump as a one-line diff · sample `/api/health` to convergence, it flaps
> ~2 minutes · if `git push` is refused, retry — it has succeeded on the third attempt · handoff §4
> owner answers are SETTLED, re-asking one is a defect.
>
> The rules this session earned: **a new page is cheaper than a third view when a guard pins the
> spelling of the second** — the squad board became its own page partly because the roadmap said so
> and partly because `tryouts_rollup.test.mjs` pins the exact text of the toggle it would have
> replaced; check what a guard pins BEFORE choosing the shape of the change. · **A guard bug and a
> code bug look identical when the suite goes red** — two of this unit's first failures were my own
> assertions pinning attribute order and a `${` that was not in the real file; read the diff before
> touching the code. · **Delegate at boot, never in render** — the positional assertion (every
> `addEventListener` at or after the boot block) is an invariant rather than a spelling, and it is
> portable to the pool board the day D-6 is fixed.
>
> **Owed by the owner (§5), ask once, do not guess:** B5 (`bt_org` localStorage vs sessionStorage —
> recommendation: keep localStorage, reversible, not blocking) · a region mapping for the area signal ·
> a fresh tester round on v0.89.0+ in Edge and Chrome · the remaining REVCO sheets from Drive
> (interactive session needed) · what an "offer" IS, which is the only thing blocking W-E.3.
>
> And above all: do not believe any document about anything you are about to build on. One check, not
> a sweep — and if the claim is about data, query live D1, then check the code that writes it.
>
> End the session properly (CLAUDE.md §7): when a context proxy fires, name which one, update the
> handoff (bumped, previous superseded and deleted), and end with a paste-ready next-session prompt
> in a fenced block.
