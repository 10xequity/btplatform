# Boomtown Platform — Handoff v0.8
**Document:** 2026-07-23_handoff_v0.8 · **Date:** 2026-07-23 · **Supersedes:** handoff v0.7 (remove v0.7 from project knowledge)
**Repo:** 10xequity/btplatform · **Live target:** v0.6.0 (code delivered as ZIP; owner uploads)

## State
- **GitHub verified this session: v0.5.0 IS live** (profile/passkey files + migration 0004 present in repo) — the owner's upload deployed.
- **v0.6.0 built this session (frontend-only, zero worker/DB risk):** member/manager sign-in tabs · site-wide role-aware sidebar (`site-nav.js`, gymdesk/volleyballlife patterns, embed-safe) · all dashboard cards clickable incl. Foundation→Settings#System · dedicated **Leagues** area · **Settings** page (account, passkeys add/remove, theme, reminders, staff System) · "← Home" on every page (schedule fixed) · **font-consistency fix** (`font: inherit` on all form controls — the mismatched-fields root cause) · admin rail parity.
- **Test data:** `db/2026-07-23_seed-testdata_v1.0.sql` written, **NOT applied**. TEST-marked, IDs 90000+, @example.com, CLEANUP block included. Apply via Cloudflare MCP (single d1 query call) or owner pastes into the D1 console.
- Docs this session: usecase-analysis-nav v1.0 · module7-install v0.6.0 · CHANGELOG v0.6.0 · this handoff.

## Last verified working
- `node --check` clean: app.js, site-nav.js, settings.js, leagues.js, admin-nav.js.
- register.js confirmed to accept `?event=ID` (Leagues CTAs link correctly).
- `/api/schedule` confirmed to return `type`, `org_name`, `price_cents`, `registered_count` (leagues.js fields match the live SELECT).
- settings.js only calls endpoints that exist in v0.5.0 (`/api/me`, `/api/profile/me`, `/api/profile/reminders {opt_in}`, btPasskey list/enroll/remove, logout).
- Worker + migrations untouched; scheduler test suite still not re-run (nothing server-side changed).

## Next steps
1. **Owner:** docs/2026-07-23_module7-install_v0.6.0.md — upload 15 files, optional seed, 7-point try-it list.
2. **Apply seed via Cloudflare MCP** (next session, 1 call) if owner didn't paste it.
3. **v0.7 (worker session):** 24h reminder cron (toggle is live, sender is not) · seeding admin UI (API live) · verified change-email flow (Settings currently routes to staff by design) · settings passkey `list()` response field names — settings.js renders `nickname/device/created_at` defensively; confirm against webauthn.js and tighten.
4. **Standing gaps:** waiver text STILL PLACEHOLDER (register flow + profile.js WAIVER_TEXT) · rate limiting · error monitoring · backup/restore UI · PWA manifest · league_weeks/sub-finder (Phase 2 — Leagues page is their landing spot).

## Gotchas
- site-nav.js self-styles and auto-wraps `<main>`/`#app`; it skips entirely on `?embed=1` (widget unaffected). Any NEW page: just add the script tag after config.js.
- Login tabs are UX-only — both roles use the same auth; role comes from the server. Passkeys still only work on 10xequity.github.io (rpId), never on previews.
- Seed rows use explicit IDs 90000+; NEVER renumber them. Cleanup = the commented DELETE block, nothing else.
- GitHub MCP write = 403 (unchanged) → ZIP + owner copy-paste remains the deploy path. Cloudflare MCP = DB work.
- Standings seed rows 2/3 deliberately create a head-to-head vs point-diff tiebreak case (noted inline) — useful for verifying the standings sort.

## Resume prompt (paste to start the next session)
"Read project knowledge (handoff v0.8, design spec v0.1, design-system v1.0, ux-copy v1.0). Repo should be at v0.6.0 — scan GitHub to confirm my upload, then: (1) apply db/2026-07-23_seed-testdata_v1.0.sql via Cloudflare MCP, (2) build v0.7: reminder-email cron, seeding admin UI, verified change-email flow. Same rules: additive migrations only via Cloudflare MCP, ZIP delivery, click-by-click for me, date+version on every file."
