# Boomtown Platform — Handoff v0.7
**Document:** 2026-07-22_handoff_v0.7 · **Date:** 2026-07-22 · **Supersedes:** handoff v0.6 (remove v0.6 from project knowledge)
**Repo:** 10xequity/btplatform · **Live version target:** v0.5.0 (code delivered; owner uploads)

## State
- **v0.5.0 built this session — Module 6: Member Profiles + Family Accounts + Passkeys.** Delivered as ZIP; owner uploads to GitHub (Step 1: create R2 bucket `boomtown-avatars` FIRST — deploy fails without it).
- **Database:** migration **0004 v1.1 APPLIED LIVE** via Cloudflare MCP and verified: `member_profiles`, `guardianships`, `signatures` (shared ledger — Module 7 contracts write here too), `season_points`, `webauthn_credentials`, `webauthn_challenges` + 6 indexes. Migrations 0001–0003 untouched. NEVER re-run any migration.
- **Decisions locked (this session):** module version **v0.5.0** (build prompt said v0.4.0 — that was already taken by scheduling/admin) · **passkeys replace TOTP** (owner found TOTP too hard; passkeys are stronger + easier) · **email reminders only, SMS deferred** (~$5–10/mo + A2P registration when wanted) · **full family accounts** (parent account, child profiles beneath, parent signs everything on-behalf with age captured, 18th-birthday handover moves history via contact_id).
- **Docs produced:** design-system v1.0, ux-copy v1.0, profiles use-case analysis v1.0 (all in project knowledge), install doc v0.5.0, this handoff, CHANGELOG v0.5.0 entry, migration record db/…0004…v1.1.sql.

## Last verified working
- `node --check` clean on all 6 JS files (3 worker, 3 web).
- Passkey byte-parsing test suite (CBOR, COSE, DER→raw, base64url, authData): **7/7 pass** (run in container this session).
- Live D1 pre-checked before migration (no table collisions) and post-verified (all 6 new tables present).
- GitHub scanned: repo at v0.4.0, matches previous handoff exactly, no drift. wrangler.toml, app.js, config.js, web/index.html all read from the live repo to guarantee integration (session key `bt_token`, org key `bt_org`, api() pattern preserved).
- Cropper.js 1.6.2 verified present on cdnjs at the exact URLs referenced.
- Scheduler test suite: not re-run this session (scheduler.js untouched; index.js changes are additive route mounting + an extracted helper with identical behavior). Re-run on next worker-code session.

## Next steps
1. **Owner:** follow docs/2026-07-22_module6-install_v0.5.0.md (bucket → upload → verify → try-it list).
2. **v0.5.1 (small, next session):** reminder SENDER — a Worker cron that emails opted-in members 24h before their events (the toggle + consent timestamp are live; the scheduled send is not). Add seeding admin UI (endpoints exist: POST /api/seeding/recompute, GET /api/seeding). Add a "Profile" card/link on the app.js dashboard (dashboard text is stale — still says "Module 4 coming soon").
3. **Module 7 (contracts):** templates with {{merge_field}} brackets auto-filled from member_profiles/contacts + custom fields, guardian routing for minors via guardianships, all executions into the existing `signatures` table. Schema needs only a `contract_templates` table (additive 0005).
4. **Standing gaps (best-practices list):** waiver text is STILL PLACEHOLDER (now in two places: register flow + profile.js `WAIVER_TEXT` const — replace both when owner supplies official text; minor-signing wording should be attorney-reviewed) · rate limiting · error monitoring · backup/restore UI · PWA manifest.

## Open decisions
- SMS reminders: deferred until owner wants to pay (~$5–10/mo + A2P 10DLC registration lead time).
- Multi-org profiles: current model = one profile per (org, contact); fine while orgs share one member base. Revisit if orgs diverge.
- Points formula for seeding (win=10, podium +50/+30/+20) — owner-tunable in ONE place: `eventPoints()` in profiles.js.

## Gotchas
- **R2 bucket must exist before the v0.5.0 deploy** (wrangler binding `AVATARS` → bucket `boomtown-avatars`).
- **Passkeys only work on the real site** (rpId = 10xequity.github.io). Not on localhost/previews. Email link is always the fallback. If a passkey login returns the counter-security error, the credential may be cloned — remove it via /api/passkey/remove and re-enroll.
- Children are `contacts` WITHOUT email (by design). Age-out sets their email + ends guardianship + sends a magic link; history follows contact_id automatically — nothing is copied or moved.
- Minors: profile visibility is capped at "members" server-side; default is 'private' with Instagram hidden.
- GitHub MCP: repo WRITE returns 403 — delivery stays ZIP + owner upload. Cloudflare MCP = database work (established); R2 bucket creation via MCP was attempted but needs in-chat approval, so it's an owner dashboard step instead.
- ICS files emit TZID=America/Denver with a full VTIMEZONE block; event times remain "as entered" (no TZ math) — fine while everything is Colorado.
- avatars: keys (not blobs) in D1; old avatar object deleted on replacement; serve path validates key shape to block bucket walking.

## Resume prompt (paste to start the next session)
"Read the project knowledge (handoff v0.7, design spec v0.1, design-system v1.0, ux-copy v1.0). Repo 10xequity/btplatform should be at v0.5.0 — scan GitHub and /api/health to confirm my upload deployed, help me fix anything red in Actions, then start v0.5.1: the 24-hour email reminder cron, the seeding admin screen, and the dashboard Profile link. Same rules: additive migrations only via Cloudflare MCP, ZIP delivery, click-by-click for me, date+version every file."
