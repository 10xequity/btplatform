# Boomtown Platform — Session Handoff v1.1
**File:** docs/2026-07-23_handoff_v1_1.md · **Date:** 2026-07-23 · **Supersedes:** docs/2026-07-23_handoff_v1_0.md
**Purpose:** everything a fresh Claude session needs. Read this + CHANGELOG. Older handoffs are archived — do not re-read.

## 1. State of the build
- **Live = v0.6.0.** Three unpasted releases, in strict paste order: **v0.7.0 → v0.8.0 → v0.9.0** (ZIPs `2026-07-23_boomtown_v0_7_0.zip`, `…v0_8_0.zip`, `…v0_9_0.zip`).
- **v0.7.0:** League Manager (2-level-gap scheduler), Sales & Reports, notifications + bell, teammate connect/invite, retry-payment, daily cron, staff assignment, nav v2.0, tokens v0.2 contrast fixes, member dashboard.
- **v0.8.0:** Control Center (GET /api/admin/dashboard; KPI row, Today & Next Up, Money Outstanding w/ inline Remind/Rerun, 7-day trend, alerts), admin.css streamline. Gymdesk pattern study — original implementation.
- **v0.9.0 (this session):** Check-in & Attendance — migration 0006 (attendance table + events.checkin_token), worker checkin.js (roster w/ waiver flags, tap toggle w/ undo, walk-ins, mint/rotate self-check-in token, public token GET/POST w/ email roster-match, /api/profile/attendance), door page admin-checkin.html/.js (auto-selects today's event, team-grouped big-tap cards, NO WAIVER flags, search, walk-in modal, QR via qrcodejs CDN + copy + rotate), public kiosk web/checkin.html (single-file), nav Check-in item.
- **⚠️ Migrations 0005 AND 0006 NOT applied to live D1.** Cloudflare MCP has been unauthorized for three sessions (tools no longer load). Apply in order 0005 → 0006 via re-authorized MCP or owner D1 console (module 8 doc §1, module 10 doc §1). Seed `db/2026-07-23_seed-testdata_v1.0.sql` also pending.

## 2. Environment facts (do not rediscover)
- Repo **10xequity/btplatform** → 10xequity.github.io/btplatform. GitHub MCP write = 403 → ZIP + owner web-editor paste; binaries via "Upload files". Worker `boomtown-api` auto-deploys on push (GitHub Actions). Cron `0 15 * * *`.
- D1 `boomtown-prod` (6cde5d11-4199-4e57-b10f-2b7e968264ea), R2 `boomtown-avatars`. Brevo + Square sandbox until keys set. Cloudflare MCP = DB only.
- Owner non-technical: click-by-click docs, date+version on every file, no filler, [FACT]/[INTERPRETATION] labels, verify UI steps by search.
- Worker pattern: `xxxRoutes(request, env, url, ctx)` + `wireXxx(helpers)`. Score POST {winner:'a'|'b', diff≥1}. Teams bulk {names:[…]}. Passkeys rpId = 10xequity.github.io. site-nav skips ?embed=1. Cache-bust ?v= (0.9.0 on new pages).

## 3. Roadmap (owner-approved order)
1. ~~M10 Check-in & attendance~~ ✅ built this session.
2. **M11 Memberships & recurring billing** — plans table, Square subscriptions, auto-retry, plan status on member profile, MRR on Control Center. Biggest revenue unlock — NEXT.
3. **M12 Marketing & comms** — segment blasts (level/program/lapsed — lapsed now computable from attendance), lead capture, automations. Uses Brevo.
4. **M13 POS-lite** (Square quick sale + inventory) → **M14 Waitlists & capacity** → **M15 PWA + push**.
5. Phase-3 carryovers: forum, photo galleries, rate limiting, error monitoring, verified change-email, passkey list() confirmation, REAL waiver text (blocks go-live), Brevo key + DKIM, Square production keys + webhook. Member attendance history UI (endpoint exists; surface on profile page — quick win).

## 4. Files in v0.9.0 ZIP
Worker: index.js (v0.9.0), checkin.js (NEW v1.0).
Web: admin-checkin.html (NEW), assets/admin-checkin.js (NEW v1.0), checkin.html (NEW), assets/admin-nav.js (v0.8.0).
DB: 2026-07-23_0006_attendance_v1.0.sql (NEW, pending apply — after 0005).
Docs: CHANGELOG v0.9.0, module10 install doc, this handoff.

## 5. Chat hygiene (standing instruction)
Context is near the 80% threshold at the end of this session — **start the next session in a NEW chat from this handoff.** First actions there: (1) re-auth Cloudflare → apply 0005 + 0006 + seed, (2) confirm owner pasted v0.7–v0.9 and health = v0.9.0, (3) begin M11.
