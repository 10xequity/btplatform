# Boomtown Platform — Session Handoff v1.3
**File:** docs/2026-07-24_handoff_v1_3.md · **Date:** 2026-07-24 · **Supersedes:** docs/2026-07-23_handoff_v1_2.md
**Purpose:** everything a fresh Claude session needs. Read this + CHANGELOG. Older handoffs are archived — do not re-read.

## 1. State of the build
- **This session discovered and fixed a broken pipeline:** the v0.7.0 ZIP was never pasted (repo went v0.6.0 → v0.8.0 → v0.9.0). index.js v0.9.0 imports `leagues_admin.js` + registrations exports that didn't exist → **Deploy Worker Actions runs failed on the v0.8/v0.9 pushes; live API was stuck at v0.5.0** while the v0.8/v0.9 frontends were live. Verified via GitHub Actions API + Cloudflare MCP (deployed bundle inspection).
- **Fix = v0.9.1 recovery release** (ZIP `2026-07-24_boomtown_v0_9_1.zip`, unpasted at time of writing): rebuilt `worker/src/leagues_admin.js` v1.1 (HARD >2-level-gap rule, stranded detection, week gen/remove, staff assignment), `registrations.js` v1.2 (sendEmail/escapeHtml/waiverReminderSweep exports — sweep chases missing waivers on rosters ≤14 days out, 1 email/48h; retry-payment w/ fresh idempotency key; teammate connect/invite; GET /api/profile/teams), `index.js` v0.9.1 (health string), `web/admin-league.html+js`, `web/admin-reports.html+js`, `web/home.html+js` (member dashboard), `site-nav.js` v2.0 (My Dashboard + Notifications badge), `tokens.css` v0.2.1 (form-control/dark-dropdown/visited-link/17px fixes), `db/…0005…sql` RECORD file (0005 already applied — never run), CHANGELOG v0.9.1, install doc. All worker code: node --check ✓, scheduler suite ✓, esbuild bundle ✓ (mirrors the failing Actions step), pairing unit tests ✓.
- **Verification after paste:** Actions "Deploy Worker" green AND /api/health = v0.9.1. Until then, treat the live API as v0.5.0.
- Menu consistency: fixed — admin-league/admin-reports use the shared admin rail (the nav's League Manager & Sales & Reports links 404'd before). Member pages all share site-nav v2.0.
- Lost with v0.7.0 and intentionally NOT rebuilt (nothing references them): logo.jpg (binary), register.js connected/invited summary, event-screen staff select (League Manager owns staff assignment now).

## 2. Environment facts (do not rediscover)
- Repo **10xequity/btplatform** → 10xequity.github.io/btplatform. Owner pastes via web editor from ZIPs; binaries via "Upload files". **GitHub MCP READ works; Actions status readable unauthenticated via api.github.com (curl) — check runs BEFORE assuming live = repo.** Worker `boomtown-api` auto-deploys on push to worker/** (deploy-worker.yml: test then wrangler). Cron `0 15 * * *`.
- Cloudflare MCP works: D1 `boomtown-prod` (6cde5d11-4199-4e57-b10f-2b7e968264ea) query + **workers_get_worker_code** (deployed-bundle inspection — how the v0.5.0 discovery was made). R2 `boomtown-avatars`. Brevo + Square sandbox until keys set.
- Migrations 0005 + 0006 APPLIED live; TEST seed live (IDs 90000+, CLEANUP block in db/2026-07-23_seed-testdata_v1.0.sql).
- Owner non-technical: click-by-click docs, date+version on every file, [FACT]/[INTERPRETATION] labels, verify UI steps by search, no filler.
- Worker pattern: `xxxRoutes(request, env, url, ctx)` + `wireXxx(helpers)` {json, audit, isStaff, requireStaff, sendLoginLink, issueSession}. Score POST /api/matches/:id/score {winner:'a'|'b', diff≥1}. League endpoints: /api/admin/leagues, /api/leagues/:id/board|levels|week|week/:round|staff. Passkeys rpId = 10xequity.github.io. site-nav skips ?embed=1. Cache-bust ?v= (0.9.1 on new/updated pages; HTML pages referencing site-nav still say ?v=0.6.0 — Pages cache expires ~10 min, acceptable).

## 3. Roadmap (owner-approved order — unchanged)
1. ~~M10 Check-in~~ ✅ (works only after v0.9.1 deploy goes green).
2. **M11 Memberships & recurring billing — NEXT.** Plans table (new migration 0007), Square subscriptions, auto-retry, plan status on member profile + home.html, MRR card on Control Center.
3. M12 Marketing & comms (segments incl. lapsed-from-attendance, lead capture, Brevo automations).
4. M13 POS-lite → M14 Waitlists & capacity → M15 PWA + push (bell already live).
5. Phase-3 carryovers: forum, galleries, rate limiting, error monitoring, verified change-email, passkey list() confirmation, REAL waiver text (blocks go-live), Brevo key + DKIM, Square production keys + webhook, member attendance history UI (endpoint live; quick win), logo.jpg re-upload.

## 4. Project-knowledge hygiene (recommended to owner 2026-07-24)
Remove from project: module6/module7 install docs + 0004 SQL (superseded; live in repo docs/ + db/). Keep: this handoff, design-system v1.0, ux-copy v1.0, design spec v0.1, Website_references.

## 5. Chat hygiene (standing instruction)
Context passed the 80% threshold at the end of this session — **start the next session in a NEW chat from this handoff.** First actions there: (1) confirm v0.9.1 pasted → Actions green → /api/health v0.9.1 (if red, read the Actions log via curl api.github.com), (2) walk the module 8/9/10 test checklists against the seeded TEST events, (3) begin M11 Memberships (migration 0007 via Cloudflare MCP, additive only).
