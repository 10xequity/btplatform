/* Boomtown Platform — frontend config
   Version: v0.2.1 · Date: 2026-07-21
   The ONLY file that changes when the backend URL changes. */
window.BT_CONFIG = {
  // Filled in after the first worker deploy (Cloudflare dashboard → Workers → boomtown-api → URL)
  apiBase: "https://boomtown-api.PENDING-FIRST-DEPLOY.workers.dev",
};
